# The system backlog

What the SYSTEM is missing, as opposed to what the product has not decided. The product's open
questions are in [`docs/backlog.md`](../../docs/backlog.md); this file holds the gaps a person hits
while assembling a screen out of `components/` and `components/patterns/`.

**It exists because of one rule and one test.** The rule is that nothing is added to the system in
the middle of building a screen: a missing component, state, token or microcopy line becomes a row
here and the screen is assembled from what is already there. The test is step 7 of the Design System
stage, which built `ui-visual/terms.html`, the first page of a TYPE the system had never drawn. A
system that grows a part every time it meets a new page has not been tested by that page; it has
been edited by it.

Every row says what was missing, what the screen did instead, and what the substitute cost, measured
where a measurement was possible.

**Standing today: fifty rows opened, twenty six closed, twenty four open.** Fifteen of the twenty six closed in the seven days of the atom migration - S30, S35, S38, S39, S40, S41, S42, S44, S45, S46, S47, S48, S49, S50 and the carried half of S41 - and every one of them closed by being MEASURED rather than by being argued. **S37 stood in that list and is open**, which is what a set of numbers typed beside a table does when nothing computes it; it is removed here rather than in a rewrite of the sentence. **Two of the fifteen closed against their own text**: S30 predicted that a check would retire with the artefact it was named after and it did not, and S42 predicted that closing it would drain S22, which is a different folder. A row is a measurement of the day it was written, and the close is where it is allowed to be corrected. Three closed the same
afternoon they were written, and by the same fix, because they were one gap seen from three sides:
the system had no reading layout. Three more closed on 2026-08-03 by the merge - S16, S17 and the
carried S11 - and all three were the same defect in different files: one shape declared more than
once and kept in step by hand. **S14 closed the same day and by a different kind of fix**: not a
merge and not a new part, but a role read at the wrong level of its own family, repaired by changing
two words. S21 closed the same day and became gate 34, and opened S23 in doing so, because a
column nothing computes is a different gap from a cell somebody typed wrong. S18 closed the same day and by the same
kind of fix as S16 and S17, which is the fourth instance of one shape under two names. Of the twenty four
that remain, S9, S12 and S15 are answers rather than gaps, so the real count is **twenty one**, and most of them belong to stages 10 to 12 rather than to this one.

---

## Opened by step 7, building Terms of Service (2026-08-03)

Fourteen sections, 1,611 words of body, assembled from `feed`, `notice`, `related`, `button`,
`header`, `footer`, `bottomnav`, `trustbar` and the `browse-shell` pattern. **No new class, no new
token, no new state, no new rule.** The page passes every gate and measures clean in both themes at
360 and 1440, including all 18 tab stops. What follows is what it cost to get there.

**Then the cost was paid, in the system.** S1, S2 and S5 were fixed the same day and the screen was
rebuilt onto the result by changing CLASSES and nothing else: `components/toc.css` is a new
component, `components/patterns/browse-shell.css` grew a reading column and learned that its rail is
a SLOT rather than a category rail, and `components/tokens.css` gained one primitive. `terms.html`
carries no style of its own, which was the test of whether the fix was in the right place. **Nothing
else in the product moved: 104 screens at two widths, 84,836 element boxes compared against the
previous stylesheet, 0 moved.**

| # | What the system does not have | What the screen did instead | The cost, measured |
|---|---|---|---|
| ~~S1~~ | ~~A reading measure~~ | **CLOSED 2026-08-03** by the reading layout | The number in the original row was wrong and is corrected here: 792px holds **89 characters**, not 122. `1ch` of the 13px prose face measures **8.89px** in the browser, and the first figure came from the 0.5em rule of thumb instead of the browser. Both are over the 60-75 `DESIGN.md` decided, so the finding stood, but a measured row may not carry an estimated number. Closed by `--container-doc` (600px, a NEW primitive beside `--container-read` rather than a correction of it, because that one is a two-column plate on ten feed screens and moving it would change ten shipped pages to fix one) and by `.read-col` reading it. **Measured after: 592px, 67 characters.** |
| ~~S2~~ | ~~Paragraph rhythm in a prose block~~ | **CLOSED 2026-08-03** by the reading layout | Two levels, and both are owned by a CONTAINER rather than by a block, which is the whole point: a margin on `.feed-seo` would be a fact about that block, so the next block dropped into the stack would arrive with no distance again. `.read-col` gives 16px between the fourteen blocks and resets their own vertical margins; `.read-col .feed-seo` gives 8px between paragraphs inside a section. The diagnosis that found it is worth keeping: the chain declares its rhythm once, in `base.css` on `.feed-inner`, and on this page `.feed-inner` had **one child**, so the declaration reached nothing |
| S3 | **An inline link style for body prose.** No component styles `a` inside a paragraph | Moved the link OUT of the sentence into a standalone `.related-more` below the box | Measured at **1.8:1** in the browser's own blue before it was moved, and reported by `audit.cjs` as "link with no rule". The product has 992 links and every one of them lives inside a component that styles it; a paragraph is the first place that was not true |
| S4 | **A breadcrumb.** No class, no component, nowhere in `components/` | Block B2 was **not built** | `ia/docs/blocks.md` banks B2 as MVP and `ia/docs/pages/seo.md` section 6 requires `BreadcrumbList` schema for this type. Both are unsatisfied. The five static pages are a SET and a breadcrumb is how a reader learns that |
| ~~S5~~ | ~~A sticky table of contents~~ | **CLOSED 2026-08-03, in full**, by `components/toc.css` | All three halves of block B5 now exist: anchored rows that are real `<a>` (so the internal-linking plane in `seo.md` section 6 is satisfied), sticky at 214px from 900px up, and collapsed above the body on mobile. The last one was built open first to test whether the bank was being cautious, and it was not: at 360 the fourteen rows pushed the document's own H1 entirely below the fold. **One thing in it is not driven:** `.toc-link[aria-current="true"]` is styled and nothing sets it at run time, because marking the section a reader is inside is a scroll position, which is Stage 11's question and not B5's |
| S6 | **A neutral page-level callout.** Nothing says "read this before the page" without a meaning attached | Used `.spinner-box` for the prototype marker, `.reconcile-box` for the effective dates and `.protect` for the money answer | Three blocks are wearing a component named for something else: a loading placeholder, a price reconcile and a funds-protection line. It renders correctly and it reads correctly, and the class names now lie on this page. That is a naming debt, not a paint defect |
| S7 | **A prominent trust paragraph.** `.protect` is fine print at **11px** | Used it for block B7, the money answer | B7 is the differentiator of this page type: the bank puts it above the contents because our two closest competitors score 1 of 5 on the funds question. It is now the smallest text in the document. The block is in the right PLACE and the wrong SIZE, and there is no larger variant |
| S8 | **Microcopy for a document page.** `voice/docs/microcopy.md` has no rows for this type | The six labels shipped as document CONTENT, not as interface strings, and none was added to the table | The six: `On this page`, `The other documents`, `Last updated`, `Effective ...`, `What changed`, `A question about this document`. `CLAUDE.md` says a UI string gets a row before it ships; these are headings inside a document rather than control labels, which is the reading that let the page ship, and it is a reading a second static page will test |
| S9 | **A registered slug.** `seo.md` gives `/legal/terms`; the painted tree is flat | The file is `ui-visual/terms.html` | Not a defect, recorded so the two are never taken for a disagreement. The slug is production's answer and the filename is the prototype's, the same split every screen in this tree already has |

### What did NOT need anything, and is worth saying

Six of the ten MVP blocks landed with no substitute at all: the header in its logged-out variant
(B1), the H1 (B3), the document sections (B6), the sibling documents (B9), the contact line (B10),
and the footer plus bottom nav (B11). The two-stone plate, the display face, the groove between
sections, the theme, the focus ring and the four bottom-nav slots all arrived by linking one
stylesheet. **The shell of a page type the system had never seen cost nothing.** Everything in the
table above is about the BODY of a long document, which is the one thing this product had never
asked for.

### The two LATER blocks, not built and not forgotten

B12 (download / print) and B14 (region note) are marked LATER in the bank, so their absence is a
plan and not a gap. B13 and B20 were refused by the bank's own rule 1 and are recorded there.

---

## Opened by step 7, in the instrument

| # | What was wrong | Where it is now |
|---|---|---|
| S10 | **A gradient is not a background-colour.** `ground()` composites `backgroundColor` up the ancestor stack, and an element painted with `background-image:linear-gradient()` computes `backgroundColor` to transparent, so the walk went straight past it and measured the label against what was BEHIND the button | Fixed in `ui-kit/_verify/browser.cjs` as lesson 8, with the case that produced it. `.auth-btn.primary` reported **1.18:1** and the flat `.cta-bar` button **1.05:1** against a brass gradient that actually measures about 5.5:1. Both false, both on a page that has shipped for two stages. Marked UNMEASURABLE rather than failing, the same answer lesson 7 gave for `mix-blend-mode`. Checked against a known real defect afterwards: `ui-kit/overview.html` still reports its 3.51:1 |

---

## Opened by the pinned-box check (2026-08-03)

Lesson 9 of `ui-kit/_verify/browser.cjs` was written for one rail and found three, then two more
things worth writing down. What it looks for: a `sticky` or `fixed` box that needs more window
height than `SHORTEST_VIEWPORT` (640px, the 1366x768 laptop less its browser chrome) and has no
scroll of its own. The three rails are fixed; these are what the fix left behind.

| # | What the system does not have | Where it stands |
|---|---|---|
| S12 | **One place that says how far a rail clears the chrome.** `top:120px` is typed into three components: `catnav.css` `.subcat`, `betpanel.css` `.bet-panel` and, until today, `toc.css` `.toc` | **It cannot be derived, and the system has already decided that.** The number is the app header (59px) plus the condensed category strip (54px, `header.css:126`) plus 7px of air, and neither of the first two is a token: the strip is a literal in `header.css`, and the header's 59px is CONTENT height, so no token could hold it. `ui-kit/docs/architecture.md` line 121 already rules this case out of the scale in words: "a number is a step only up to 64px; above that it is a layout position and it stays literal". So it stays literal, and this row is the reason rather than a comment beside each copy. What the row is FOR: the three copies can drift, and today they were already meant to differ. `.toc` is now 66px because a document page has no category bar for the strip to condense, which was measured and not assumed |
| S13 | **A specimen frame is a window, so a component capped to the window caps inside it.** | The vitrine sizes each `.ck-frame` to its content and the content now sizes itself to the frame, which settles (four identical loads agree) but settles SHORT: the `catnav` rail specimen went 499px to 430px and the `betpanel` one 554px to 516px, so the vitrine shows those two components with their feet behind an internal scroll. It is truthful, since a frame that size IS a short window, and it is a worse showcase. The answer belongs to the vitrine (a declared height for a rail specimen), not to the component, which is why nothing was changed in `components/` for it. 25 snapshots across 5 pages, no product screen among them |
| ~~S14~~ | ~~**The odds-bar labels are under the floor on 12 feed screens, and the light theme is the bad half.**~~ **CLOSED 2026-08-03**, and it was never a token value question | **The cause was a role at the wrong level of its own family.** `oddsbar.css:13-14` put `--outcome-yes` and `--outcome-no` - the FILL roles, the 4px band and the track behind it, which answer to 3:1 and take it - onto 12px bold TEXT, which answers to 4.5:1. The system already had `--outcome-yes-text` and `--outcome-no-text`, added in stage 08 and looking redundant at the time. The fix is **one word in each of two declarations**; no token moved and no value was edited, and the bar did not change by a pixel. Re-measured in a browser at 390px on `ui-visual/event-feed.html`: `span.l-yes` daylight **2.62 -> 7.23**, `span.l-no` daylight **3.76 -> 7.57**, `span.l-no` graphite **4.35 -> 7.54**, `span.l-yes` graphite **6.23 -> 9.82**. All four changed, including the one that already passed, because leaving it on the fill role would mean one component reading a different token per theme for the same word. 266 label elements on 105 painted screens. `ui-kit/docs/history.md` records why this is the best evidence the run produced for having a semantic token level at all. **The row as it stood on the morning of 2026-08-03**, and it is kept because the count was itself a correction: the full count was **378 elements below 4.5:1**, in three equal groups of 126: `span.l-no` at **4.35:1** on graphite, `span.l-no` at **3.76:1** in daylight, and `span.l-yes` at **2.62:1** in daylight. `span.l-yes` on graphite passes. 12 screens, both widths, `oddsbar.css:14`. The row used to say "4.35:1, 0.15 under, a token value question" because that is what the audit printed, and the audit printed six findings per render and stopped: **32 of the 48 renders with anything to say hit that cap exactly**, so the number a person read was the cap. `audit.cjs` counts them all now and says what it dropped. 2.62:1 is not a rounding question, it is the YES label of every card being half the required contrast in one of the two shipped themes, and `DESIGN.md` owns `--outcome-yes`. Pre-existing: measured identical at HEAD before this pass and after it |

---

## Opened by the scrollbar pass (2026-08-03)

| # | What is missing | Where it stands |
|---|---|---|
| S15 | **A painted scrollbar cannot be measured on this machine**, so `--scroll-thumb` is verified by its declared value and not by its pixel | The platform draws an overlay bar the compositor owns, and it is outside what a screenshot captures. Proved rather than assumed: the thumb was forced to `#ff00ff` and five frames taken during an active scroll of the rail contain **0 magenta pixels**, so no colour would have shown, not just this one. Chrome's classic-scrollbar flag does not help, because on this OS the choice is the system's and not the browser's, and no second engine is installed to cross-check. What IS measured: `scrollbar-width` computes to `thin` on all eight vertical scrollers, `scrollbar-color` computes to the role in both themes, and the role measures 3.43:1 and 3.39:1 against the ground every one of them sits on. A machine that shows classic bars would close this in one screenshot |
| ~~S16~~ | ~~**One quiet button, five names**~~ **CLOSED 2026-08-03**, and in two pieces | Six of the names were not a merge at all: `.btn-primary`, `.btn-secondary` and the `.btn-sm`/`.btn-md`/`.btn-lg` ramp stood on 0 screens in both trees, so they were DELETED and **gate 30** now fails the build on any class the system declares and the product does not wear. The four the product does wear were merged: one rest block for `--bg-control` + `1px --border-hairline` + `--text-primary` + `--radius-10`, which four rules had been writing separately, and one hover block on `--bg-control-hover`, which is what the majority already used. **Rest moved nothing: 525 of 525 snapshots identical.** Hover moved 19 measurements and every one is named in the same commit |
| ~~S17~~ | ~~**The header's icon circle is declared twice**~~ **CLOSED 2026-08-03** | The three declarations that ARE the circle - transparent ground, `1px --border-hairline`, `--radius-pill` - are one block naming `.icon-btn` and the two `<summary>` elements, next to the hover and the press that were already gathered. Each element keeps its own geometry below, because a 36px square and an inline-flex row with a gap are not the same thing. Not a decision about a compact logged-out header: the two circles are identical in both states and it was the NEIGHBOURS that differed |

---

## Older, carried

| # | Item | Where it came from |
|---|---|---|
| ~~S11~~ | ~~`.app-case .cta-bar button` is styled by `account.css`, not `button.css`~~ **CLOSED 2026-08-03** | Eight rules moved to `button.css` with the rest of the family, and the one line that was arrangement rather than paint went to `patterns/action-bar.css`, which already said how an anchor child shares the row. `account.css` is now the bar and nothing else, which is why it joins `_levels.STATIC`: a bar answers no pointer. **This does NOT close item 16d in `docs/backlog.md`.** That row is a boundary question - `account.css` is a CTA bar and a transaction list in one file - and moving a button's paint out of it makes the file thinner without making it one component |

---

## Opened by the merge (2026-08-03)

| # | What is missing | Where it stands |
|---|---|---|
| ~~S18~~ | ~~**Two roles, one value, one job**: `--border-brass-hover` and `--line-brass-soft`~~ **CLOSED 2026-08-03.** Merged through `ui-kit/_rescale.py` `ROLE_RENAME`, which renames the declaration and every reader and then drops the duplicate per block: **9 rewrites across 8 component files**, one token gone, no value edited. **The name that survives is the one that names its STATE**, because `--border-brass` is the same border at rest and a state is a token in this system; `soft` named an intensity in a family whose `strong` member is also a hover, and that confusion is what let one job carry two names. The original row: | Both are `--brass-a45` on graphite and `--brass-a60` in daylight, in every theme, and both are described in `tokens.css` as the hover border of a quiet control ("the hover border of the quiet button family" and "the quieter hover border"). `--line-brass-soft` has 9 readers and `--border-brass-hover` 3. The button family stopped reading the second name today, because a merged rule can only carry one; the other 8 readers were left alone, because renaming a role across 9 files is a token decision and not a button one. It costs nothing to look at and it is exactly the shape S16 was: one thing under two names, kept in step by hand |
| S19 | **A quiet control's hover ink is not in the family's contract** | `.auth-btn`, `.state-btn`, `.provider-btn` and now the action bar's button all take `--text-strong` on hover; the brass ones keep `--text-on-brass`, and that had to be RESTATED in the brass hover rule once the quiet rule started setting a colour, or the first child of an action bar read white ink on lit brass. It works, and it works because two rules agree about a third thing. Whether the ink belongs in the state at all is a DESIGN.md question: nothing else in this product brightens its label under a pointer |

---

## Opened by the name pass (2026-08-03), and closed as a decision rather than a task

| # | What is missing | Where it stands |
|---|---|---|
| S20 | **Five component and class names carry the word the product's own dictionary bans.** Measured across all 37 component names against `voice/docs/voice.md`, `voice/docs/microcopy.md`, `ia/docs/sitemap.md` and `DESIGN.md` | **MEASURED, AND DELIBERATELY NOT PAID.** The names: `components/position.css` and `components/patterns/position-list.css`, named for a word the Lexicon lists under *Not allowed in the UI (trader jargon): position (for the user's own bet)*, drawing exactly that on 36 screens; and `.hold-row` / `.hold-name` / `.hold-amt` / `.hold-out`, `.ptable`, `.ed-panel-holders` and `.ed-panel-positions`, which are the vocabulary `voice/docs/microcopy.md` Step 14 took out of the COPY and left in the classes. **The size, so nobody has to guess later:** the `.pos*` and `.ptable` classes are **1,930 uses in 129 files** (`position.css` 68, `active-bets-history.html` 52, `my-profile.html` 51); the component NAME is **149 mentions in 26 files** of registries and documents; the `hold*` / `ed-panel-*` set is **935 uses in 23 files**. **Two reasons, and the second decides it.** First, `voice.md` forbids these words **in the UI**, and a class name is not UI: the screens say *bet* and *Biggest bets* everywhere a person can read, which is what the ban is for. Second, gate 18 pairs the two trees on `tag.firstclass`, so renaming a class is an edit to `wireframes/` as well, and the grey tree is frozen against regeneration - the generators that would carry it are the ones `CLAUDE.md` forbids running, so the rename would be 1,930 hand edits across two trees to fix a word no user sees. The cost is real and the benefit is a grep. **What this row buys:** the next person to grep `holder` and find the system, or `bet` and find the product, has this line instead of a mystery |

---

## Opened by the authored fan-out (2026-08-03)

| # | What is wrong | Where it stands |
|---|---|---|
| ~~S21~~ | ~~**Three inventory rows do not match the product, and all three come from the half of `ui-kit/_fill_inventory.py` that is not computed**~~ **CLOSED 2026-08-03, and it became gate 34.** All three cells corrected: L163 to `position.css`, L155 to `profile.css`, L97 to 11. The mechanism was closed rather than the instances: `stems_for()` now tries every dot-separated part of a selector left to right instead of taking the first, so `p.pos-status` resolves to the class and not to the tag, and a row can no longer fall silently through to the hand map. **Gate 34 rebuilds the three computed columns and fails on any cell edited away from what the markup says, and the hand map gained the idle control - which found EIGHT dead rows of forty one on the run that added it**, every one in the same hand-written half both false cells came from. Proved both ways: one file cell edited by hand goes red, one hand-map row that matches nothing goes red, and the rebuilt table is byte-identical with the eight deleted. The original row: | Found one per round while writing the authored sources, which is the useful part: nobody was looking for them. **L163** files the wallet's transaction list under `components/account.css`, which has two rules and no list; it is `components/position.css`. **L155** files the section divider `p.pos-status` under `components/position.css`; it is styled in five rules of `components/profile.css` and `position.css` does not mention it. **L97** says `.ed-layout` stands on 10 screens; it is 11, in the painted files and in R3 and R7 both. **The mechanism, and it predicts the next one.** The generator's docstring says most of the table maps itself from the `Classes:` header and that `TITLE` is "the only hand-written part". Both false FILE cells are in `TITLE`, and L155 did not have to be: `stems_for()` extracts a class with `c.lstrip(".").split(".")[0]`, so a tag-qualified selector resolves to the TAG. `p.pos-status` becomes `p`, nothing owns `p`, and the row falls silently through to the hand map. **Four rows in the table are written that way and three are right by coincidence** - `dialog.app-dialog` and `dialog.outcome-dialog` twice, where the tag happens to be a component name. The fourth is the defect. L97's number is in the SCREENS column, which no generator computes at all, while `ui-kit/patterns/index.json` and two rules of use already hold the true count. Closing it is two lines in `stems_for()` and one more computed column, not three hand edits |
| S22 | **19 MB of screenshots are in git, and the rule written to keep them out cannot reach them** | `ui-kit/screens/README.md` records the decision plainly: 118 MB of full-page PNG "would put 118 MB into every clone to prove a sentence this file already carries", so `.gitignore` took `ui-kit/screens/*.png`. Two things it does not do. **A `.gitignore` never untracks**, so the **29 PNG** that were committed before the rule was written are still tracked: **14 MB**. And the rule names one extension, so the **16 JPG** before/after pairs of the merge pass walk straight through it: **5.3 MB**. Total **19 MB** in a 62 MB pack, for images whose own README says they should not be there. Not deleted here on purpose: removing tracked files is not a tidy-up |
| S23 | **The inventory's `#f` column is not computed by anything, and it cannot be until it has one definition** | Opened by gate 34, which deliberately does NOT gate this column. Measured on 2026-08-03: of the 88 rows, **54 quote a class that is findable in the painted tree, and 38 of those 54 disagree** with a count of the files carrying it. The disagreements are not all errors, and that is the finding: some cells mean **files that carry the markup** and some mean **screens where a person sees it**, and for an overlay those are different numbers by construction - `.signin-dialog` is emitted into every one of 105 screens and is the state of 4, and the cell says 4. Others are plainly stale (`.subcat` says 32 against 105, `.tabs` says 9 against 18, `.cmt` says 36 against 9) and three are ranges or open-ended (`10-36`, `9+`, `10+`), which no generator can write. **Closing it is a definition first and a column second**, and the definition is a product question about what the table is for: an index into the system wants "where do I see this alone", a census wants "how much of the product would this change touch". Until then the column is hand-written and says so, which is better than a computed number answering the wrong question |
| ~~S24~~ | ~~**One button under five names, and the system's largest open structural item**~~ **CLOSED 2026-08-05, by doing it** | **Five names, three axes.** Size (3 font steps, 12/13/14, over 4 paddings), width (fit against full), and emphasis - which is expressed **three different ways**: a modifier (`.primary`), a POSITION (`.cta-bar > *:first-child`), and a name of its own (`.confirm-btn`). **`components/button.css` is 30 rules: 6 write the anatomy and the axes onto a name, 11 are the state layer and have to be scoped, and 13 are RECONCILIATION** - a scope re-saying what a name already said. **Four of those 13 exist only to put an axis back** (`.resolved-panel .state-btn{width:100%}`, `.bet-sheet .confirm-btn{padding:16}`, `.bet-dock .confirm-btn{width:auto;padding:12}`, and the `.cta-bar` position rule), and **one re-declares the whole anatomy from nothing in 16 declarations** (`outcome-dialog .provider-btn`). The stand pays for it in pictures: **8 photographed faces x 4 states x 2 themes = 64 shots**, where the axes say there are 2 faces, so a matrix page is 2x4x2 plus a rest strip of 3 sizes x 2 themes = **22, which is 42 fewer**. **WHY IT IS NOT BEING DONE, and it is the measurement that says so:** the three axes do not fit inside `components/button.css`. The position mechanism belongs to `components/patterns/action-bar.css`'s arrangement and the `width:100%` belongs to a `state-block` context, so consolidating touches a pattern and two components - a refactor of the centre of the system on the step that closes it. And the right answer for emphasis is `.primary` in the MARKUP, which is frozen: gate 18 pairs the trees on `tag.firstclass`, so giving the system axes its own markup cannot express would be worse than the names. **Closing condition: together with the markup thaw, not before.** What is paid instead is one page: `ui-kit/button.html` now leads with the axis matrix, so a person arriving for a button leaves knowing which one, and the page now answers with AXES instead of a list of places. **Updated 2026-08-04, and still open:** the page was rebuilt to specify four axes (size, width, emphasis, icon) with a rule on each and a verdict on every combination the product does not have, and the 64 shots became 40 because a gallery is now a DIFFERENCE rather than an occurrence (gate 36). **None of that is the consolidation.** The css is unchanged, the 13 reconciliation rules are still 13, and the closing condition is still the markup thaw. What changed is that the page stops teaching the five names as five components **DONE.** The closing condition was the markup thaw and the thaw came: the five names are `.btn` plus an emphasis, a size and an optional `.btn-block`, in the stylesheet and on **710 placements over 105 painted screens and 703 over 104 grey ones, in one change**, which is the condition gate 30 states and the condition the first attempt at this vocabulary failed. Both objections above were answered rather than waived. The position mechanism stayed in `patterns/action-bar.css`, which is where it belongs and where it still is; the three width overrides became one class the person placing the control can read; and the emphasis is a class of its own rather than `.primary`, so gate 18's pairing on `tag.firstclass` sees `btn` on both trees and pairs harder than before. **30 rules became 20 declarations, 13 reconciliation rules became 0, 11 scoped selectors became 1**, the component drew 19 faces at 1440 and draws 13, and the 40 shots are 24. What is left of the page is the matrix, on ONE specimen instead of two, because a form is a set of classes and is no longer a place |
| ~~S25~~ | ~~**The size axis is a fact with no rule behind it**~~ **CLOSED 2026-08-05** | Opened 2026-08-04 by rebuilding `ui-kit/button.html` around its axes. Three font steps, 12/13/14, and **nothing in the product predicts which one a place gets**. Measured in a browser at 1440 and 360: container height does not predict it (the state block is 245 tall and takes the smallest step, the action bar is 70 and takes the middle one), and the share of the container looks monotone until two placements break it in both directions - `.resolved-panel .state-btn` is full width at 12 and `.bet-dock .confirm-btn` is fit width at 14. **The step follows the NAME**, which is the same finding as S24 seen from the page rather than from the css. Worse, one class carries two steps: `.provider-btn` is 13 in its own rule and **14 inside the sign-in and outcome sheets, which is 322 of its 444 uses**, so the size a person meets is not the size the stylesheet declares first. The page says "a fact without a rule" in those words rather than inventing one, because an invented rule is worse than a missing one. **Closing condition: with S24, or by a decision that the three steps are two** **DONE, and the rule is that the step is a CLASS.** Three font steps with nothing predicting which a place got are now `.btn-sm`, `.btn-md` and `.btn-lg`, asked for by name, and the two steps that were 13 are one: sm is 12px and md and lg are 14px. The proof that the axis is now an axis is a measurement of the box rather than of the font, over all 105 painted screens at both widths: **sm is 36px on all 194 of its single-line readings, md is 47 on all 1,028, lg is 55 on all 8.** One value per size and no exceptions, where the same sweep taken the day before returned 34 AND 36 for sm and 45 AND 47 for md, because the base had no edge and the quiet skin did, so the size was reading the emphasis. That is fixed at the base with a transparent 1px band; see `components/button.css`. `.provider-btn` carrying two steps at once is gone with the name: the sheet scopes that re-declared it are deleted |
| S26 | **Two combinations of the button axes are named and unsettled** | Opened 2026-08-04 by the empty-cell pass over the axis matrix. Neither is forbidden and neither is decided. **Disabled anywhere but `.confirm-btn`**: the product disables exactly one control in each tree and it is a confirm button, so there is no answer for what a disabled QUIET button looks like - `--opacity-disabled` would apply and nobody has looked at it on a 1px edge. **A marked control at fit width inside a sheet**: the icon rule (a mark only where the label names a third party) and the width rule (full when the control owns its row) each rule out half of it and neither rules out the whole. Both are written into `ui-kit/authored/button.md` as UNCLOSED ZONE with this number, so the matrix has no blank cells |
| ~~S27~~ | ~~**Five components still photograph an occurrence as a difference**~~**CLOSED 2026-08-04, and the number it closed on is not the number it opened on.** | Opened at 64 pictures, which was an arithmetic assumption rather than a count: it multiplied the redundant groups by 8 and three of them do not hold 8 (a `card` group holds 6, because a card is not focusable, and a `tabs` group holds 4, because a hidden input raises no hover and no press). **The real upper bound was 56.** All five were re-captured under the extended face and the outcome per component was decided rather than counted: `card` merged two pairs (12 pictures), `hero` one (8), `event-detail` one (8) and `header` one group of four (24); **`tabs` merged nothing** and became `ui-kit/docs/defects.md` row 48 instead, because its pair differs by `opacity` and the difference is two ways of hiding one radio in one file. **44 of the 56 were duplicates, 4 were a defect, and 8 were a real difference the old instrument could not see** (`header`'s `.bal-add`, which stayed). System 766 pictures to 714, 94 groups over 24 components, and `NOT_RECAPTURED` in `ui-kit/_check_kit.py` is now empty, so gate 36 gates every component unconditionally |
| S28 | **`.ed-act` is dead markup: 27 uses in each tree, read by no stylesheet and no script** | Found 2026-08-04 while re-capturing `event-detail` for gate 36, and it is the whole reason that component had two galleries instead of one. The rule that paints the row is `.ed-actions button`, which matches with or without the class; a strict grep over `components/` returns no rule reading `.ed-act`, and the page scripts hook `[data-fav]` and `[data-comments]` rather than the class. So the two faces were identical by construction and the class was the only thing telling them apart. **It is markup**, so under the twin contract it belongs to `wireframes/` first and to Stage 12 handoff rather than to a Design System edit, which is the same route `ui-kit/docs/defects.md` row 46 takes. Both trees agree on all 27, so gate 18 is quiet and correct. **Closing condition: delete the class in the grey tree and port, or give it a rule.** Checked and NOT the same case: `.prov-google` is also read by no rule, and correctly so, because that mark carries its own brand colours in the markup while `.prov-x` and `.prov-apple` are filled with the current colour and need one |
| S29 | **`ui-kit/tokens.html` scrolls sideways at 360** | Found 2026-08-04 by the browser pass over the stand pages, and left rather than chased because it is a different cause from the three that were fixed the same day. Those three were an inline `code` with nothing to break at, closed with one rule in `ui-kit/_page.css`; this one is **+214px** and the widest boxes are a token name in `b` beside a prose column in `span`, so it is a layout question about the token row rather than a wrapping one. Measured: 4 of 56 stand pages overflowed at 360 before the rule, 1 after, 0 at 1440. **Closing condition: give the token row the same treatment `.tk-doc-tbl` already has, a box that scrolls in itself rather than a page that scrolls sideways** |

---

## Opened by the states rebuild (2026-08-04)

| # | What is wrong | Where it stands |
|---|---|---|
| ~~S30~~ | ~~**714 photographs are still captured, stamped and gated, and 710 of them are shown by nothing**~~ **CLOSED 2026-08-07, and the prediction written into this row was wrong** | Opened 2026-08-04 by rebuilding the States section of every component page around what the capture MEASURED rather than around what it photographed. `ui-kit/_verify/states.cjs` writes two things per state per theme: a png, and the nine values the face was read at. The pages now read the values, and a picture survives on exactly the four `disabled` shots, which is the one state a reader cannot raise for themselves on a live frame. The other 710 are on disk, in git, hashed by `ui-kit/_states.py --stamp` and held by gate 31, and no page links one. **Why they were not deleted here.** Deleting them is a change to the INSTRUMENT and not to the pages: `states.cjs` has to stop calling `shoot()` for the three raisable states, the manifest has to be rewritten without those `file` keys, and that is a browser, a served tree and a full re-capture of 24 components. Gate 31's crop half (`no picture crops its subject`) and the three declared entries in `_states.TIGHT` retire with them, because a frame that holds nothing cannot be short. **Closing condition: one re-capture run, with the two gate halves retired in the same commit.** Until then the pictures are the provenance of the numbers, which is true, and is also what a person says about something they have not got round to deleting. **The pictures went on 2026-08-05**: `shoot()` takes a null path for the four raisable states and measures without writing, **722 png became 4 and 14.6 MB became 384 KB**, and the four that survive are the four the pages render, `cookie-consent/g2-disabled-*` and `input/g2-disabled-*`. **What this row predicted did not happen, and that is the more useful half.** It expected gate 31's crop half and the declared entries in `_states.TIGHT` to retire with the pictures, on an argument that sounds like one: a frame that holds nothing cannot be short. The clip is COMPUTED whether or not it is written, because the pad a subject needs is the same question with or without a file at the end of it. **4 of 658 readings are still short**, both subjects are still declared, and the gate has been passing for two days on a measurement whose artefact stopped existing. That check was never about the png: it asks whether the STAND gives a control the room its own paint needs outside its box, which is why both survivors are focus rings. **So what retired was the noun.** 87 groups, 658 readings, 4 pictures, and until today every count printed by `_states.py` and three of gate 31's six halves called all three a `picture` - a gate reporting "0 of 658 picture(s)" over a tree holding four, which is the small version of the thing this file exists to catch |

| S31 | **Gate 38 asks one component whether the system covers the product, and the question belongs to all 37** | Opened 2026-08-04 with `ui-kit/_worn.py`, which compares the button forms the painted tree wears against the forms the vitrine stages, in both directions, and found two on its first run: the `.resolved-panel` pair, the only place in the product where a state button is full width, standing on one screen and staged nowhere. **The unit is the reason it is worth generalising.** Gate 30 already asks whether a declared class is worn, and `.provider-btn` passes it while being four different controls: the scope decides the paint, so one class is 13px and left-aligned in a deposit sheet, 14px centred with a brand mark in the sign-in sheet, 14px bold in an outcome sheet and 13px semibold in the bet panel. A stand that showed the class showed one of the four and every gate stayed green. **What is already general and what is not.** The reader takes the family and the scopes out of `components/<name>.css` rather than from a list, so pointing it at another component is a matter of naming its stands; what is button-only today is the `STANDS` constant and the assumption that a component's forms fit in one or two specimens. **Closing condition: the same comparison running for every component with a stand**, which is the pass that also has to answer what a form IS for a card, a chip and a panel, where the scope may not be the thing that varies |

| S32 | **The header specimen cannot show its own account menu** | Opened 2026-08-04 by the census windows, which frame each control through a hole onto the specimen it already stands in. The account dropdown is 196 wide and anchored to the right of the header; in `header-in` the header runs to the document edge, so **98 of those 196 pixels are painted past the viewport** and are not there to be framed. The window came back half menu and half nothing. **The windows themselves were deleted the same day** and the census now slices each control out with `pick()` and stands it on the page ground, so `NO_WINDOW` is gone with them; the finding is not, because the dropdown is navigation and left the button page for `header.html`, where it still cannot be shown open. On a painted screen the same menu fits, because `.app-case` is centred with margins, and there it ships closed. **Closing condition: the header specimen gets a case with margins, the way the product has, or the dropdown gets a stand of its own.** The second is probably right: a menu that only exists open is a state, and every other state in this vitrine has a specimen |
| S33 | **A change to `browser.cjs` for any reason invalidates all 746 state pictures** | Opened 2026-08-04, the same day the file was added to `_states.ALWAYS`, and the addition was correct: that file holds what a FACE is, how long a state settles before it is read and how a frame is derived from its subject, so it decides a picture as much as `states.cjs` does and the hash could not see it. The cost is over-invalidation, and it was paid three times in one afternoon: adding `boxOf()` for the census windows touches nothing a state picture depends on and re-stamped 98 groups anyway, at about 35 minutes a run. **Two ways out and both have a cost.** Hash a SLICE of the file, which means naming which functions decide a picture and keeping that list true, so the drift moves from the hash to the list. Or split the file, which is the move this folder made once already and would undo: one reader of the browser was the whole point. **Left open on purpose**: a gate that is too eager is a cost, and a gate that is too narrow is a defect, and this repo has paid for the second more than for the first |

| S34 | **The product draws 66 different faces of one kind of control, and no scale decides any of them** | Measured 2026-08-04 in a browser, every button-shaped control on all 105 painted screens at 1440 and 360: **9,648 readings, 4,811 controls on the wide pass, 66 distinct faces**, where a face is the fifteen values the stylesheet decides and not the width its container gives it. The vocabulary is the finding. **17 paddings** for what is one kind of thing, **22 rendered heights** from 17px to 71px, **7 font sizes**, **4 weights**. Three radii, and those three are the one axis that is a decision (10 plate, 100 pill, 0 none). One class is not one control: `.provider-btn` measures **7 faces**, the bare `<button>` 23, `.sel` 5, `.confirm-btn` 3. And the four scopes of `.provider-btn` agree on padding, radius, ground and edge and disagree on **font size and weight only** - 14/600 centred in the sign-in sheet, 13/400 left-aligned in a deposit one, 14/700 in an outcome sheet, 13/600 in the bet panel - which is not four decisions, it is one decision taken four times by four people. **Accessibility rides on the same finding**: 438 of 4,811 placements declare a 44px minimum, and controls at 17, 18, 22, 24 and 26px ship. **Closing condition: a declared size ramp with every existing padding mapped onto it and the leftovers named as deliberate exceptions, plus a gate that fails a face outside the ramp.** Not started here on purpose: every line of it moves pixels on 105 painted screens, so it is a decision per item rather than a sweep. This entry is the measurement the decisions will be taken against |
| ~~S35~~ | ~~"This one is selected" is spelled five ways, and the loudest of the five is against `DESIGN.md`~~ **CLOSED 2026-08-06 by measuring all of them and then reading what the files said.** The row counted five spellings. Read in the browser at 1440 in both themes, **the loudest of the five had never rendered**: `.cat-nav li[aria-current] button` set a full brass GRADIENT at (0,2,2), and every `.cat-nav` in the product and in the vitrine sits inside `.feed-inner`, whose rule is (0,4,2). Measured `background-image: none` on the current chip before it was deleted, along with a `border-width:2px` that was dead in the same way. **Two spellings were live**, not five: `--tint-brass-09` inside a `--tint-brass-45` edge with `--text-brass-chip` and a faint halo on **815** category chips, and `--tint-brass-16` with `--text-brass` and no halo on **543** - the rail chip, the bet panel's quick amounts and the deposit sheet's. **Neither file gave a reason.** `tokens.css` carries only a provenance for each - *the condensed category chip when active*, *a selected quick amount* - which is what a value read off an already-painted screen looks like, and is exactly the distinction `--outcome-yes-fill` DID make when it wrote *spectator, not trader*. **So the majority won**: one selected chip is `--tint-brass-09`, `--text-brass-chip`, bold, the halo, and a `--tint-brass-45` edge WHERE THE CHIP HAS ONE. It is also the safer pair in daylight, where both inks collapse to `--brass-700` and the other ground is the deeper of the two. **The recipe colours what a chip already has and adds no parts**, so the rail chip keeps no edge and no box moves. **525 snapshots, 55 differ, 121 elements, three properties: `color`, `backgroundColor`, `boxShadow`.** No box, no border width, no padding, no font. **The state gallery merged the deposit sheet's chip back together with the bet sheet's**, which is the second time this month it has checked a caption that a person wrote. S38 and S41 are unblocked; the chip PARTITION - four files to one - is the next pass and is now a move at zero | Measured in the same pass. The selected state of a chip is: a **brass gradient** (`.quick button.sel` in the deposit dialog, 107 placements), a flat brass tint at **.16** (`.quick button.sel` in the bet panel and sheet 11, `.seg` 9, `.ed-range` 9), at **.09** (`.cat-nav` 54, `.cat-condensed` 105, `.feed-subfilter` 1), at **.16 in oklab** (`.tabs` 9) and at **.18 in oklab** (`.subcat` 32). `DESIGN.md` decides this in two places and both say tint: "active is a brass tint (`rgba(199,162,78,.08)` fill)" and the One-Accent Rule, "brass carries identity, the active state, and primary CTAs only... if a screen has brass on more than the logo, the active chip, and the primary action, it is overspent". **A solid brass gradient is the primary action's own face**, so the 107 quick chips wear it while the 11 that follow the spec are the minority. Two more near-duplicates in the same family: `.seg` and `.ed-range` are identical in padding, radius, size, height and selected ground and sit **one weight step apart** in both states; `.tabs` and `.cat-condensed` agree on radius, size and height and differ by **4px of horizontal padding**. **Closing condition: one tint, one alpha, one weight step, and the gradient reserved for the action.** Blocked on the same decision-per-item as S34 |
| S36 | **The vitrine paints its own emphasis in the outcome red, on all 37 component pages** | Found 2026-08-04 by `node ui-kit/_verify/audit.cjs --kit` while checking that the rebuilt button page had introduced nothing. `ui-kit/_page.css` line 564 is `.ck-anti>b{color:var(--outcome-no)}`, so the word that opens every anti-rule is drawn in the NO red. Two things are wrong with one declaration. **It is below the floor in both themes**: 3.69:1 in daylight and 4.3:1 in the Vault, measured on the anti card's own ground, on every page that has an anti-rule. And it is the rule this repo states first: green and red are OUTCOME semantics in this product, an accent never borrows the win/lose colour, and a warning in a document is not an outcome. The correct role is the brass one the rest of the vitrine uses for emphasis, or a dedicated warn role if a document needs one. **Closing condition: one declaration and a re-measure**, and it is left out of the button-page pass on purpose, because it is 37 pages and belongs with whatever else `_page.css` owes |
| S37 | **A state's ink is shown on a ground it does not have, and the caption says otherwise** | Same audit run. The States section paints a `--text-*` role as `.ck-st-chip.ck-ink`, which sets `background:var(--color-action)` for every one of them, under the caption "the label, on the ground this state puts it on". Brass is the ground for exactly one of those roles. `--text-strong` is the QUIET button's hover ink and sits on `--bg-control-hover`; drawn on brass it measures **2.41:1 in the Vault and the same in daylight** and looks like a defect in a token that is fine. So the swatch is the strongest kind of wrong: it is a picture of a value, on the wrong ground, with a sentence claiming it is the right one. **Closing condition: the chip takes its ground from the same state row the ink came from**, which the generator already has in hand, and the audit's contrast count drops by the number of `--text-*` rows in the system |
| ~~S38~~ | ~~**A selected chip answers no press, anywhere in the product** **AND IT IS NOT ONLY THE CHIP, 2026-08-07.** The favourited bookmark tile has the same shape on the other axis: `.icon-btn-tile[data-fav][aria-pressed="true"]` is (0,4,0) against its hover's (0,3,0), so a saved tile answers no POINTER either - saved rest and saved hover read byte for byte in both themes, raised by a real click. Winning the finger is deliberate there and written down, since brass leaving under a press would read as the favourite coming off before the click had said so; winning the pointer was never decided at all. **So the closing condition grows by one: whatever a chosen control says under a finger, a chosen control also has to go on saying it is a control under a pointer.** Five controls now, on two axes.~~ **CLOSED 2026-08-07, AND THE ANSWER WAS ALREADY IN THE PRODUCT.** This row guessed at a deeper tint and a token of its own. Measuring every CHOSEN control instead - the rail chip, the amount, the category, the lane, the option row and the saved bookmark, at rest, under a real pointer and with the pointer held, in both themes - said otherwise twice over. **First: `components/yesno.css` had already written down the rule this system has, THREE press mechanisms, one per kind of surface.** A quiet control settles onto `--bg-pressed`, a brass gradient reverses its own angle, a filled colour goes down by depth as an inset shade. A chosen chip is a TINT, which is nearly nothing over the surface beneath it, so its ink was never measured on the tint and it takes the quiet control's answer - the one the `[aria-current]` chips already had, decided and captioned a day earlier. **Second: nothing but a `:not(.sel)` made the two halves differ.** So the fix deletes an exclusion rather than adding vocabulary: `.app-case .chip:active{background:var(--bg-pressed)}`, one selector, every chip, chosen or not, keeping its brass edge, its brass label and its halo. The saved bookmark took the same answer at its own weight, because `[data-fav][aria-pressed="true"]` is (0,4,0) and was outranking the press as well as the hover. **AND THE POINTER IS NOT A GAP, which is the correction this measurement forced on S45's close.** That close called the saved tile a control that had stopped saying it was one. It is not an outlier: **not one of the six chosen controls answers a pointer**, in either theme, and the uniformity is the decision - a chosen control's whole face is already the answer, and a second one under the pointer would compete with it. That sentence is now written in `components/chip.css`, `components/quick.css` and `components/iconbtn.css` where the `:not(.sel)` hovers are, so the next person reads a decision rather than an omission. **`.opt-row` was checked and is not in this count**: it is a `<div>` that holds a YES / NO pair, so a container with no states is correct. | Opened 2026-08-04 as a consequence of the change that took the deposit sheet's chosen quick amount from a brass gradient to the brass tint every other selected chip wears. The gradient carried a hover and a press with it, because a lit plate has a three-step; a tint has no plate, so those two rules went with it. What that exposed is that they were the ONLY ones: `.seg button.sel`, `.ed-range button.sel` and `.quick button.sel` in the bet panel have never had a `:hover` or an `:active` of their own, so on a phone, where there is no hover at all, tapping an already-chosen chip says nothing. **It is uniform now, which is the improvement and also the whole finding**: one gap in one place, instead of one place answering and three staying silent. **Closing condition: one answer for a selected chip under the finger, declared once and applied to all four.** The obvious candidate is a deeper tint rather than `--bg-pressed`, because a pressed chip that loses its brass reads as being deselected, and that would need a token of its own |
| ~~S39~~ | ~~**The target-size rule exists, is right, and has never been measured**~~ | Opened 2026-08-04. `components/tabs.css` states the rule in its own comment: target size follows the POINTER and not the viewport, 44px under a coarse pointer, 36px under a fine one, which clears WCAG 2.5.8 with room. The rule is correct and at least two files were moved onto it in step 7. **Nothing has ever checked the other thirty five.** Measured at a FINE pointer on all 105 painted screens: 22 rendered heights, of which 44 and up account for about 1,400 of 4,811 placements, and three controls do not clear 24x24 in any pointer regime - `.cmt-actions button` at 17px, `.sheet-grab` at 18px, `.toggle` at 22px. The coarse-pointer measurement, which is the one the rule is actually about, has not been taken. **HALF CLOSED 2026-08-05.** The coarse-pointer sweep was taken: a real touch context at 390px over all 105 painted screens, 4,837 placements, `matchMedia('(pointer: coarse)')` confirmed true before every read. **79 placements failed WCAG 2.5.8 and now 0 do**, and it was three controls and no others: `.cmt-actions button` at 23x17 on 72 placements, which is both axes and is the smallest control in the product; `.sheet-grab` at 390x18 on 4; `.toggle[role=switch]` at 40x22 on 3. Fixed in the controls' own boxes rather than with an invisible pseudo stretched over them, because a hit area no measurement in this repo can see is a fix nobody can check. Exactly 79 boxes changed and not one other. The switch also gave up `--icon-22` for `--size-24`: the size of a MARK was being asked for the height of a control. **WHAT IS STILL OPEN IS THE 44, and it is the larger half: 2,472 of 4,837 placements are under it, unchanged by the above, because 25 and 24 are over the AA floor and nowhere near the rule this file's own comment states.** Social marks at 28x28, the sheet close at 32x32 (one control since 2026-08-05, where this line counted two), the header's entries at 54x36, the subcategory chips at 41, three pixels short. **Closing condition: every control at 44 under a coarse pointer or on a declared list, then a gate.** It was deliberately NOT being done before the button reclassification, because a size ramp sets those heights anyway and doing it first moves the same pixels twice. **THE RAMP LANDED 2026-08-05 and the blocker is gone**, which also turns the remaining half into a much smaller question than the sentence above implies: `.btn-md` renders 47 on all 1,028 of its single-line readings and `.btn-lg` 55, so **the whole button family clears 44 except `.btn-sm`**, which renders 36 on 194 and is 8px short in one place - a padding step of one class, not a sweep over 4,837 placements. What is genuinely still open is everything that is not a button: the marks, the two closes and the chips **CLOSED 2026-08-07, and what closed it was one declaration in the place the rule had never been written.** `components/button.css` had no floor at all while six other files carried one, so two regions were keeping a copy for one control each: `components/profile.css` for Edit and `components/tabs.css`, `components/header.css`, `components/iconbtn.css`, `components/chip.css` and `components/filters.css` for their own. `@media(pointer:coarse){.btn{min-height:var(--control-44)}}` on the family, and the per-control copy in profile deleted. **Measured before and after on 105 painted screens in a real touch context at 390px, `matchMedia('(pointer: coarse)')` asserted true before every read: 209 boxes were under 44 in HEIGHT and now 0 are** - 137 readings of `sm` at 36 and 72 of `xs` at 25 - while `md` at 47 and `lg` at 55 never moved. It is written on `.btn` rather than on the two steps because a floor is the FAMILY's: a seventh step added later gets it without anybody remembering. **What is left is width and it is not a sweep**: `.btn-bare`, the comment row's Reply and Like, is 31 to 37px wide by 44 tall, which clears WCAG 2.5.8 (24x24) and not 2.5.5 (44x44) on one axis. Padding a two-word text press to 44 wide would be a face invented to satisfy a number, so it is written down here rather than done. The marks and the two closes named in this row were all fixed in the icon-button migration on 2026-08-06 |
| ~~S40~~ | ~~**A face is nine properties and the focus ring is not one of them**~~ **CLOSED 2026-08-05** | Opened 2026-08-05 by merging the product's two close buttons. `ui-kit/_verify/browser.cjs` `face()` reads background, background-image, border colour and width, ink, radius, transform, box-shadow and opacity. It does not read the OUTLINE, so two controls that answer a pointer identically and answer the KEYBOARD differently are one gallery. Measured on `how-it-works.html` with the element actually focused: the close disc rings `rgb(215,172,83)` on every dialog head in the product and `rgb(255,255,255)` on the how-it-works head, and in daylight `rgb(104,79,24)` against `rgb(33,31,25)`. That is not a rounding difference, it is brass against white, and the rule behind it is measured and correct: `.hiw-glow` puts brass in the corner the ring lands in, so a brass ring reads 2.52:1 there. **The states pass reported 4 faces from 10 occurrences and 4 from 9 before the merge**, which is the instrument saying nothing changed while a whole state moved. It is the same shape as the `transform` gap recorded in `ui-kit/authored/button.md`, found the same way: a value the face key does not carry is a difference the gallery cannot show. **It is not a one-line fix, which is why it is a row and not an edit.** Adding `outlineColor` to `face()` is not enough on its own: the grouping key is computed at REST and in one theme, and at rest `:focus-visible` does not match, so the property would read the element's default rather than the value the ring rule sets. The key would have to be taken in the focused state, or the ring would have to become a fifth axis beside the four states. Either changes `browser.cjs`, which is in `_states.ALWAYS`, so it re-captures all 714 pictures and re-cuts every gallery in the vitrine. **Closing condition: decide where the ring belongs in a face, then one re-capture.** Two components override the ring today, `components/course-chrome.css` and `components/dialog.css`, so the churn is bounded and knowable before the run. **CLOSED THE SAME DAY, and by one property rather than the redesign this row expected.** The row said adding `outlineColor` to `face()` could not work on its own, because the grouping key is taken at REST where `:focus-visible` does not match. That is true of the FIRST key, which only decides what gets shot together, and this row was written from it: `states.cjs` then merges on a signature of **all four states in both themes**, and that is where a ring difference lands. So rest reads no ring for anybody and no group is lost, focus reads the real one and the group that should split splits. **93 groups became 94**, and the one that opened is exactly the one this row named: `button.sheet-close @dialog.hiw-dialog`, ringing `rgb(255,255,255)` where every other close disc in the product rings `rgb(215,172,83)`. The hand-written paragraph in `ui-kit/authored/dialog.md` that existed to say the instrument could not see it is now that group's caption. **79 of the 94 digests name the ring**; the other 15 have no focus reading at all, and **not one focusable group in the system draws no ring** - the first time this repo has had that as a measurement rather than as a rule. Four values and not one: style, width, colour and offset, because a colour with no width is a ring that may not be drawn. A ring that is not drawn is also not allowed to MOVE: `outline-color` defaults to `currentColor`, so the first run reported that 12 groups move the ring on hover, which is a value read where no rule was ever written |
| ~~S41~~ | ~~**The atom shelf was never examined, and four of its seven entries are not atoms**~~ | Opened 2026-08-05, and it is the first row of a larger question the user asked in one sentence: why does the system have a section in ATOMS for Load more, a toggle inside sort and filters, and a skeleton inside inputs. The answer is structural and it is two things. **The components were not designed, they were READ out of already painted screens** - `ui-kit/kit.html` says so about itself, "the flat kit the system was read out of" - so a component here equals a css FILE, and the files were cut by what happened to appear on a screen. **And one number was doing two jobs**: `ui-kit/_levels.py` computes containment for the cascade, which is the only right answer to "a part is imported before the whole", and `CLAUDE.md` then reused that same number as the atom / molecule / organism level, which is a design decision and not arithmetic. Gate 39 closed the second half on the day this row was written: an empty containment is now a question the build asks, and the atom shelf went from 7 to 3. **What is still open is the CUT.** `filters` holds `.toggle`, a switch, beside `.filter-menu`, a disclosure over `.filter-panel` over a list of labelled radios: two control families, and the stand already says so by needing two specimens. `input` holds `.field-label` and `.amount-input` beside `.quick`, which is a GROUP of chips. `skeleton` arranges `.sk-line` and `.sk-thumb` with `.sk-head` and `.sk-row`, and declares `.sk-btn`, a placeholder named after another component's control; it is also a STATE rather than a control family. `loadmore` is one control and measures, over 105 painted screens, as the same graphite chip as `.cat-nav button`: same `--bg-chip` ground, same 6 per cent edge, same corner, same 14px semibold, same hover and press, disagreeing only on side padding (24 against 20), letter-spacing and min-height. `toc` stands on 1 painted screen of 105 and may not be product at all **Closing condition: each of the five cut, and the cut proved rather than asserted.** `filters` into a switch (atom) and a filter menu (molecule); `input` into a field (atom) and a quick-amount group (molecule); `skeleton` decided as a state or as a component, not both; `loadmore` dissolved into the chip family; `toc` moved to vitrine furniture or kept with a reason. Every one of those moves a class between files, so it moves the cascade, so each needs `ui-kit/_verify/snap.cjs` before and after with the diff at zero - the same proof the 2026-08-05 raise itself was held to. **This is step B of the plan; step A was gate 39.** Steps C (the vitrine page template) and D (the reverse of gate 30: every block the PRODUCT wears is declared by the system, which is what "pages are composed from the design system" means as a check) are not open as rows yet, because both are decided after the cut **PARTLY CLOSED 2026-08-05, and the remainder is one migration rather than four cuts.** `.toggle` left `filters.css` for `components/toggle.css`, an atom that had been filed inside a molecule because they shared a file name, and the stand had been saying so since it was built by needing two specimens for one component. `.quick` left `input.css` for `components/quick.css`: a field and a group of chips are not one thing, and `ui-kit/_worn.py` had already filed them under different ROLES. Both cuts were proved with `snap.cjs` before and after, both trees. Two more were EXAMINED and owe nothing: `skeleton` is right as a file and was only wrong as a level, because `.sk-btn` standing where a button will be is the entire job of a skeleton rather than a name borrowed from another component; `toc` stands on terms.html, which is product, and one screen is a small component and not a wrong one. **What is left is `loadmore`, and it is not a cut.** `.load-more`, `.cat-nav button`, `.seg` and `.ed-range button` are one graphite chip drawn in four files, and no file can be moved to fix that: a file that styled another component's classes is the thing S11 forbids. It needs the shape the buttons got - one marker class, modifiers, and the markup of both trees moved in the same change as the stylesheet - over roughly 1,500 placements. That is a migration and it gets its own step, with its own measurement. **THE FIRST CUT OF IT LANDED 2026-08-05, and the measurement is what decided its size.** The four are not one chip, they are two families and two strays: `.seg button` and `.ed-range button` were the same declaration block BYTE FOR BYTE in two files, so they merged into `components/chip.css` with zero visual change by construction, 63 buttons per tree over 9 screens each and 7 in the frozen kit; the census went 26 kinds to 25 and two rows of 27 and 36 became one of 63. `.load-more` and `.feed-inner .cat-nav button` are the OTHER chip, a raised one on `--bg-chip` with a bevel edge at 14px, and they are four declarations apart rather than zero: padding 24 against 20, min-height 44 against 0, a letter-spacing, an inset groove. Each of those four is a decision somebody has to take, not a duplicate somebody can delete. `.quick button` is a third face and the pill `.cat-nav button` outside `.feed-inner` a fourth. **What is left of this row is 1,120 placements over two decisions**, and both are about whether the raised chip's four differences are meant **AND `quick` JOINED IT ON 2026-08-06.** Its amounts went to `components/chip.css` as the atom's `.chip-amount` face, and what is left in `components/quick.css` is `display:flex`, a gap, `flex-wrap` and a bet panel pushing the row to the right. Two components now exist that are one layout rule for one row, which is one more piece of evidence for the same answer: neither is a component, and both close by deleting a file rather than by moving another rule **THE LAST CUT LANDED 2026-08-07 AND THIS ROW IS CLOSED.** `.load-more` carries `.chip.chip-nav`, which is what this row said on the day it was opened. The four declarations it named were three nothings and one exception: `min-height:44` was INERT (12/24 at 14px is a 47px content box, so it never bound at any width in either theme), `letter-spacing:.01em` was the BUTTON family's tracking worn because the control was filed as a button, `padding 12/24` was four pixels a side, and the inset groove was a lit top edge the chip family had decided against on purpose. **Its states needed no decision at all**: the hover was `--border-brass-hover` on `--bg-control-hover` with `--text-strong`, in that order, in two files, and both pressed to `--bg-pressed`. **45 readings, 173x47 to 162x47, and the height did not move - the inert floor proving itself. Everything else on 525 snapshots: 0 resized, 0 moved, 0 properties.** The state gallery then MERGED the load-more capture with the category strip's without being asked, which is the third time this month it has checked a caption a person wrote. The role stays `action` and the atom is `chip`: a role is what a control MEANS and an atom is where its RULES live, which is the social mark's split in the other direction and now the second instance of it. **What is left of both files is a ROW each**, `components/loadmore.css` and `components/quick.css`, and both are declared STATIC. Whether a row deserves a file is a separate question from whether a control deserves a component, and it is left open on purpose: `.quick` stands in three regions - the deposit sheet, the bet panel and the bet sheet - so moving it into one of them is the thing S11 forbids, and neither file costs the atom map anything now |
| ~~S42~~ | ~~**718 of 722 state pictures are captured, hashed, gated, versioned and displayed nowhere**~~ **CLOSED 2026-08-05 by the capture, 2026-08-07 by the vocabulary** | Opened 2026-08-05 while looking at a component page and asking where the state photographs are. They are not on it, and that is deliberate and written down: `ui-kit/_gen_component_pages.py` renders a picture only for `disabled`, "the one state a reader cannot raise", because every other state can be raised by hand on the live specimen three sections up the same page. The decision is right. **The capture never followed it.** Counted: 722 png on disk, 14.6 MB, and grepping every generated page, specimen and script for a reference finds **four** - `cookie-consent/g3-disabled-*` and `input/g2-disabled-*`. The other 718 are shot on every re-capture, deduplicated, hashed by source, policed by gate 31 for freshness and gate 36 for orphans, and carried in git. They are not the source of the measured tables either: those come from `getComputedStyle` in `states.cjs`, and the grouping that decides how many faces there are is computed from the same values, not from the images. **This is also what has been throttling every css change in this repo**: a one-line edit to a component invalidates its pictures and costs a re-capture, and this session paid that toll five times for pictures nobody can look at **Closing condition: shoot what is shown.** `states.cjs` keeps measuring all four states in both themes, because the tables and the face grouping need it, and stops writing a png for anything but `disabled`. Gates 31 and 36 then police four files instead of 722, `ui-kit/docs/backlog.md` S22's 19 MB of screenshots in git loses most of its weight, and a re-capture stops being a reason to defer a fix. **Not done in the same pass that found it**, because it edits the capture and the two gates that read its output, and a pass that changes both the measurement and the check on it should not also be the pass that changes the thing being measured. **Done the next day.** `shoot(i, state, file)` takes a null path and measures without writing; the clip is computed either way, so the settled face, the box and the pad wanted against the pad got are the same numbers the tables and the face grouping always read, and all four states are still raised with a real pointer, a real Tab and the mouse really held down. **722 pictures to 4, 14.6 MB to 384 KB**, and a css edit stopped costing a re-capture of anything a person could look at. **Two things this row said were wrong and are corrected here rather than left as prose.** It expected gates 31 and 36 to "police four files instead of 722": they police **658 readings and 4 files**, because a reading is stamped, hashed and held exactly as a photograph was and only the artefact at the end of it went away. And it expected S22's 19 MB to lose most of its weight, which was a folder confusion: S22 is `ui-kit/screens/`, measured again today at **29 tracked png at 14.8 MB and 16 jpg at 5.5 MB in a 62 MB pack**, not one byte of it touched by this. S30 carries the rest of the close |
| S43 | **Nine declared lists, and four of their counts in this document were typed and wrong** | Opened 2026-08-05 while writing gate 40. `ui-kit/docs/architecture.md` prints a table of the nine declared lists with a count beside each name, and four of the nine had drifted: `TRUE_ATOM` said 3 and holds 5, `ORDER_BREAK` said 4 and holds 2, `SPECIMEN_DEBT` said 11 and holds 10, `STATIC` said 10 and holds 11. Every number was correct when it was typed and none of them is read by anything, so nothing could catch them going stale. The numbers are corrected, which is the half that does not last. **Closing condition: the counts come out of the lists.** `python3 ui-kit/_levels.py` already knows every one of them and `_adoption.py` knows the two it owns, so the fix is the mechanism `README.md` already uses for the gate count and the component count: a marked span the generator rewrites, and a gate that fails when the file stops matching. It is the same argument as gate 23 for the `@import` order and gate 30 for the coverage table: a truth computed in one place and typed in another is a truth that will disagree with itself, and this document is the one that makes that argument |
| ~~S44~~ | ~~**Two icon buttons cannot adopt the atom without changing a pixel, and the difference is the browser's rather than anybody's**~~ **CLOSED 2026-08-06 by three answers, and the answers took one afternoon because they were the only thing left.** The mark on the event action row is **18**: `.ed-actions .ic` had said `--icon-18` since the day it was written, and 14 was what 6px of user-agent padding left of it inside a 28px tile under 560. The atom's `--space-4` leaves exactly 18, so the number in the file is now the number on the screen. Its ink stays **`--text-muted`**, so the tile face takes the ink back from the atom and hands the sprite `color:inherit` rather than a second copy of the value. The toast dismiss hovers **the way the other 388 outlined circles do**, tinting its ground and taking a brass edge instead of brightening its own edge alone; it keeps its ink lift, which was never the question and which the header has no use for because the header already rests at `--text-primary`. Measured after: **525 snapshots, 50 pages, 209 elements, and every one of them named**. 54 are the 27 marks and their `<use>` growing 14 to 18 at 380px, which IS the decision. The other 155 are `padding`, `display` and the two flex alignments on boxes of fixed size holding one centred child or nothing at all, which draw nothing and are the user-agent values leaving. **No box moved and no colour changed anywhere in the product.** `iconbutton` is the first atom closed: 836 placements, five faces, **one file**, and the distance to the map fell **15 to 13** | Opened 2026-08-06 by trying it and measuring. `.ed-actions button` and `.toast-close` both set no `padding` and no `display`, so both took the user agent's: **1px 6px, and `display:block`**. Adopting the atom hands them `padding:var(--space-4)` and `display:inline-flex`, and on the event action row that is **visible**: the 18px mark was being squeezed to **14px wide** by the 6px of UA padding, so the atom un-squeezes it. `snap.cjs` reported 17,478 boxes changed across the two, and 270 marks changing colour as well, because the atom hands a sprite `--text-primary` where `.ed-actions` had handed it `--text-muted` through `fill:currentColor`. **None of those values was decided by a person.** The bookmark, whose face WAS decided, adopted the same atom at 525 snapshots and 0 differences on the same afternoon, which is the control for this experiment. **Closing condition: decide the three values, then adopt.** Is the event action mark 18 or 14; is it `--text-muted` or `--text-primary`; is the toast dismiss the same hover as every other outlined icon button (it brightens the EDGE where the header's tints the GROUND, 4 placements against 388). Each is one line, each is visible, and none of them belongs in a partition: a migration that changes a pixel is a migration that made a decision it did not declare. The two faces are written and reverted in `2d5e0a1..HEAD`, so the adoption is a re-apply once the three answers exist |
| ~~S45~~ | ~~**One rest face, two hovers, and the count is 27 against 525**~~ **CLOSED 2026-08-07, AND THE ANSWER IS NEITHER OF THE TWO THIS ROW OFFERED.** The quiet hover is not the tile giving way to the majority and it is not the surface deciding: **the tile may not spend brass on a pointer because it already spends it on a STATE.** Raised the way a person raises it, by clicking the bookmark on `ui-visual/event-detail.html` rather than by staging an attribute, a SAVED tile reads `rgb(215, 172, 83)` for its mark. The lift's hover reads `rgb(215, 172, 83)` for its mark. **The same value.** Give the tile the loud hover and pointing at an unsaved bookmark paints it the exact ink that means saved, on the one control in the product where those two answers sit a pixel apart. **So the rule that closes this is: a face answers the pointer with the accent unless the accent is already carrying a state of its own, and then it answers with what is left, an edge and an ink.** Two of this atom's six faces spend brass on a state; the social row spends none, so it may. Nothing was repainted and `components/iconbtn.css` now carries the reason where the two arguments used to sit unresolved. **It also turned up a second finding nobody asked for**, and that one is real: `[data-fav][aria-pressed="true"]` is (0,4,0) against the hover's (0,3,0), so a saved tile answers NOTHING under a pointer - saved rest and saved hover read byte for byte in both themes. Winning the finger is deliberate and written down; winning the pointer was never decided. Filed with S38, which owns the same shape on the chip. | Opened 2026-08-06 by the measurement that moved the social mark out of `navitem`. `.icon-btn-tile` and `.icon-btn-lift` are the two faces of this atom that stand on an ordinary surface, and at rest they are the SAME picture: `--bg-control`, a hairline, a 10px corner, a centred flex box, an 18px mark. Read in the browser at 1440 and 380, every one of those values matches. **They part only on the pointer.** The tile brightens its ink to `--text-primary` and firms its edge to `--bevel-strong`, leaving the ground alone. The social mark tints its ground with 20 per cent of `--color-action`, takes a `--text-brass` edge and **lifts two pixels**, settling back on press. 27 placements against 525, so the loud one is the majority, which is the opposite of how S44 read. **The question is which answer an ordinary surface gives**, and it is one line either way: the toolbar over an event head is quiet on purpose because three of them sit over a photograph, and the footer row is loud on purpose because the lift is the Vault's floating language and there is nothing behind it to compete with. Both arguments are written in `components/iconbtn.css` and neither is settled. **Closing condition: decide one hover, or declare that a face may answer the pointer by its SURFACE and not by the atom** - which is the more interesting answer and the one the five other faces already point at, since every one of them turned out to be an answer to its ground |
| ~~S46~~ | ~~**Two controls the navitem migration could not take, and both were left because one width or one value had not been read**~~ **CLOSED 2026-08-07.** The notification row adopted at zero on 2026-08-06; the sub-category row was measured at BOTH widths on 2026-08-07 and it is not this atom at all. **380: a 114x41 hairline PILL, `--border-hairline` on a 100px corner. 1440: a 206x41 full-width ROW, a transparent edge, a 10px corner, the count pushed to the far side by `space-between`.** A nav item is transparent with no edge and no corner; this carries a 1px band and a corner at both widths, so it was never one. **It is a `chip`, and it is the first control in this system whose SURFACE changes with the window**, so the face carries the media query and `components/catnav.css` keeps only the rail. It was also the last of S35's five spellings of chosen - 18 per cent of `--color-action` in oklab with a solid brass edge and semibold - and it wears the one recipe now. **525 snapshots, 165 differ, and one box resized that was not a stated decision: a chosen lane 94px wide to 95, because bold is wider than semibold.** Closing it took `catnav` out of `chip` AND out of `navitem`, and the distance 7 to 6 | Opened 2026-08-06 when `components/navitem.css` took two of the atom's kinds and stopped. Reading one width is the same half-measurement that put the social mark in the wrong atom, so it was written down rather than guessed twice |
| ~~S47~~ | ~~**The button family has three emphases and a ramp with a hole in it, and not one of its six leftover kinds can adopt without a value being chosen**~~ **CLOSED 2026-08-06, all five answered and applied the same day.** **(1)** `.cmt-post` adopted `.btn.btn-primary.btn-sm` and **grew 2px in each dimension**, which is the family's transparent band arriving: measured 50x34 to 52x36 on 7 placements. **(2)** `.cc-btn` took `md` and its font moved **13 to 14**, 46 to 47 tall on 3. **(3)** `.hiw-btn` became `.btn.btn-ghost.btn-xs`: the third emphasis and the ramp step it already wore, both added at the values the product already draws, so **105 placements moved no pixel at all**. **(4)** `.cmt-actions button` became `.btn.btn-bare`, the fourth emphasis, **72 placements and no pixel**. **(5)** `.edit` took the family's small step and changed four values on **one** element, which is the right trade for an exception of one. **525 snapshots, 431 differ, 2137 elements, and every property is named**: no `color`, no `backgroundColor` and no `backgroundImage` moved anywhere in the product, and the only boxes that moved are the 55 readings of those three controls plus the reflow above and below them. The rest is `display` block to flex, `gap` normal to 12 on single-child flex boxes, `justify-content` normal to center, `border-style` none to solid under a 0px width, and `font-family` narrowing from `"DM Sans", system-ui, sans-serif` to the `--font-body` role's `"DM Sans", sans-serif`, which changes only which fallback would be used if a font served from this repo failed to load. **`button` went 6 files to 5 and the distance 11 to 10**; the header stopped drawing a button entirely. **What it did not take, and where each is held:** `.load-more` (9), because S41's measurement says that component should not exist and the chip family is blocked on S35, so calling it a button now would pre-empt both; and `.cmt-signin` (4), whose `--bg-well` ground and muted ink are not an emphasis this family has and which the specimen corpus stages nowhere, recorded in `_states.NOT_SHOT` and the reason `.cmt-post` was kept as a name rather than deleted | Opened 2026-08-06 by reading all seven kinds out in the browser before moving anything, the way S46's two were found. 710 of 911 are already `.btn`; the table of the other 201 is in `ui-kit/docs/atoms.md`. **Five questions, each one line and each visible.** **(1) `.cmt-post`, 11 placements**, is `.btn.btn-primary.btn-sm` in every value except that it has `border:0` where the family carries a 1px TRANSPARENT band, so adopting grows it **2px in each dimension**. The band exists on purpose: `components/button.css` says it is there so the box is the SIZE's answer and not the emphasis's. Adopt and grow 2px, or keep the exception? **(2) `.cc-btn`, 3 placements**, is `.btn.btn-secondary` at **13px** against a ramp of 12 / 14 / 14. Its padding is already `md`'s 12/12, so `md` moves only the font 13 to 14, while `sm` would also move the padding. 13 or 14? **(3) `.hiw-btn`, 105 placements**, is a transparent pill with muted ink and a `--color-action` 14 per cent hover: the icon button's skin with a word in it, and the same declaration is now written in `components/header.css` and `components/iconbtn.css` both. It is a THIRD emphasis the family does not name. Name it and adopt, or leave 105 placements in the header? **(4) `.cmt-actions button`, 72 placements**, is transparent with no edge and no corner at 11px muted: a bare text press, which is the navitem's rest face wearing an action's meaning. A fourth emphasis, or not a `.btn` at all? **(5) `.edit` at 1 and `.load-more` at 9.** Naming a face for a single element is what this document forbids about the logo, and S41 already says `.load-more` should not be a component because it duplicates the catnav chip. Both may be answered by deleting a name rather than adding one. **Closing condition: answer the five, then adopt in one pass.** Together they take `button` from six files to one and the distance from 11 to 6 **AND ITS ONE LEFTOVER CLOSED 2026-08-07.** `.cmt-signin` is `.btn.btn-prompt.btn-lg.btn-block`: the family's FIFTH emphasis, and this row's own sentence about it - that a `--bg-well` ground and muted ink are not an emphasis this family has - turned out to be the argument for NAMING it rather than for leaving it in `components/comments.css`. Four placements on two screens, two of them on the SAME screen saying different things, is a kind and not an element. It is the only emphasis on a RECESSED ground and the only one muted at rest, because it is the only one that is an INVITATION rather than an action and it stands where the thing a signed-out person came for should have been. **20 readings: 54 tall to 55, and 73 to 76 where the label wraps, which is the ramp's `lg` step taking the font 13 to 14 - the same single value `.cc-btn` cost.** The diff also caught a bug in the first draft in one line: `.btn` sets no weight and a fifth emphasis that said nothing inherited the body's 400, so 20 readings went 600 to 400. **An emphasis here is a ground, an edge, an ink AND a weight.** `components/comments.css` is STATIC now and `button` reached one file: the atom map's distance is 0 |
| ~~S48~~ | ~~**Four controls in the header band wear one skin, and after two migrations that skin is written in three files**~~ **CLOSED 2026-08-07: A SURFACE MAY OWN A SKIN, AND A SKIN IS A SET OF TOKENS.** Counting is what turned the resemblance into a fact: all 242 plain `.icon-btn` in the painted tree stand in `<header>` (105 in `.left`, 137 in `.utility`) and every icon button anywhere else carries a face modifier; all 105 `.btn-ghost` are the How-it-works pill, also in `<header>`, and there is no ghost elsewhere in the product. **The skin exists in one band and nowhere else.** Measured at 1440 and 380 in both themes the four are one skin to the byte, and the only thing that parts them is the rest INK, because three are a mark at `--text-primary` and the fourth is a word at `--text-muted` that rises under the pointer. **NOT A FILE AND NOT A CLASS, AND GATE 41 IS WHAT DECIDES IT.** A fourth file drawing `.icon-btn`, `summary` and `.btn` is three more file-slots on two atoms, which is exactly what the metric counts and exactly what the last seven days took from 18 to 0; a shared class is that file with a name on it, because a face's rules live in its atom's file and the day `.band-quiet` were named in a census row the metric would start counting it in three places, correctly. **The two `<summary>` disclosures are the proof it had to be a token**: a summary is neither `button` nor `iconbutton`, so no atom could ever have held their half, and an answer that works for two of the four is not an answer. So each file keeps its own rule saying its control stands on the band, and the band says once, in `components/tokens.css`, what it does to whatever stands there. **AND NAMING THE VALUE MOVED IT ONE RUNG, WHICH IS THE FINDING UNDER THE FINDING.** The wash was written longhand as a 14 per cent colour-mix; `ui-kit/_rescale.py` has mapped 14 to 16 since the brass scale was declared, because 14 is not a rung, and it matches token NAMES - so three copies spelled as a colour function were invisible to the tool that exists to keep values on the ladder, for as long as they existed. The rewrite came back unasked within the minute of the token being named. `--tint-brass-16` is `--brass-a16` on graphite and `--brass-a30` on chalk, so the hover measured against its own band goes **1.27:1 to 1.33 in the dark theme and 1.11:1 to 1.25 in the light one**: a flat alpha does not read the same on two stones, which is the whole reason the ladder shifts, and this wash had been flat in both. **16 readings changed, every one of them `backgroundColor` inside `:hover` on the four band controls; 525 snapshots, 0 differ, 0 elements changed**, because the change lives entirely in a state no before-and-after in this repo can photograph. Nine more longhand brass tints are still out there and they are S50. | Opened 2026-08-06 by the button migration, which had to SPLIT two selectors to take `.hiw-btn` out of `components/header.css` and left the other halves behind. The band's quiet controls are the icon button (`.icon-btn`, 388), the notification and account disclosures (`.notif-menu summary` and `.avatar-menu summary`, both `<summary>` and neither in the census, because a census of button-shaped controls reads `<button>`), and the How-it-works pill (`.hiw-btn`, 105). All four are transparent on a hairline pill and all four answer a pointer with `color-mix(in oklab, var(--color-action) 14%, transparent)` and a `--color-action` edge, then press to `--bg-pressed`. **That declaration is now in `components/iconbtn.css`, `components/header.css` and `components/button.css`, three times, byte for byte.** Each copy was made deliberately and each is documented, which is what makes it a finding rather than a mess: a file may not style another component's classes (S11), so the alternative at the time was worse. **What the map has no word for is a SKIN shared across atoms.** Every atom so far has turned out to be a set of faces, and a face has turned out to be an answer to the surface the control stands on - S45 is three for three on that. A band is a surface. **Closing condition: decide whether a surface may own a skin the way a component owns a class, or accept three copies and say in each of them that the other two exist.** The two disclosures are the test: they are not `button` and not `iconbutton`, so no atom can hold their half |
| ~~S49~~ | ~~**The outcome pair is neutral at rest in the bet panel and coloured at rest everywhere else, and the panel spells its selected side two ways**~~ **CORRECTED AND NARROWED 2026-08-06, THE SAME DAY IT WAS OPENED, BY READING WHAT THE FILES ALREADY SAID.** The row below calls the neutral rest a divergence. It is not. `components/tokens.css` declares `--outcome-yes-fill` with the words **"the tinted YES button (spectator, not trader)"**, and `components/betpanel.css` argues the same distinction in its own comment: *this one rests on --bg-control with --text-muted and does not carry an outcome colour anywhere... pressing it in green would be inventing a colour the control does not have and telling the user the side is chosen before it is.* **A spectator's pair states the market's two sides; a trader's chooser states the CHOICE.** That is a decision, written twice, and `.bp-side` is a genuine second FACE of this atom rather than a control that drifted. The row below was written from a measurement without reading the reasons, which is the same mistake in the other direction from the one that put the social mark in the wrong atom. **AND THE THIRD REST SPELLING IS NOT ONE EITHER**: `--outcome-yes-fill-soft` is `var(--green-a12)` and `--outcome-yes-fill` is `var(--green-a12)`, the same value under two names, and `tokens.css` says so itself with the words "for now". A naming duplicate, not a visual one, and the file is holding the two apart deliberately. **WHAT IS ACTUALLY OPEN IS ONE VALUE.** The selected side is solid `--outcome-yes` under `--text-on-yes` in the panel and in the option list, at 4.64:1; it is `--outcome-yes-fill-strong` under `--outcome-yes-text-lit` in the dock, at 5.3:1. **Both pairs are measured and both clear the floor. Neither file says why the same control should differ at two widths** - the dock IS the panel below 760px - and the dock's comment only explains that its pair is legible, not that it should be a different pair. Two of the three placements are solid. **Closing condition: pick one selected fill, then `.bp-side` moves to `components/yesno.css` as the trader face and the outcome atom reaches one file, distance 9 to 8.** The move waits on it because the two faces would otherwise carry the disagreement into the atom's own file~~ **CLOSED 2026-08-06: solid, at both widths.** The chosen side in the mobile dock was `--outcome-yes-fill-strong` under `--outcome-yes-text-lit` and is now the solid `--outcome-yes` under `--text-on-yes` that the panel and the option list already used. A person choosing a side on a phone and a person choosing it on a desktop are choosing the same thing. **525 snapshots, 16 differ, 32 elements, and every one of them is that decision**: 16 chosen sides and the 16 odds figures inside them taking the new ink. No box moved, no padding, no font, nothing outside the dock. **The state gallery is the better proof.** It groups a face by what it MEASURES, and it had three entries for a chosen side - the panel's, the dock's and a multi-outcome row's. It has **one**. Two groups did not disappear because somebody deleted a caption; they merged because the controls became the same control. **And four tokens went with the answer.** `--outcome-yes-fill-strong`, `--outcome-no-fill-strong`, `--outcome-yes-text-lit` and `--outcome-no-text-lit` had nothing left to paint, and gate 11 said so; the four primitives underneath them - `--green-a20`, `--red-a20`, `--green-150`, `--red-250` - went one level down for the same reason. A primitive exists because a role needs it. **`.bp-side` then moved to `components/yesno.css` at zero**, as the atom's trader face: `.yesno-pick` in the panel and `.yesno-pick-bar` in the bar. **The outcome atom is one file and the distance is 8.** Two cycles were declared on the way, `(yesno, betpanel)` for the odds figure a side holds and `(dialog, yesno)` as a specimen debt, because the bet sheet is a dialog holding the pair and is staged as a bet panel. The one thing that did NOT move is what a hover moves: the bar takes a ground where the panel takes only the edge and the ink, and S49 did not ask that | Opened 2026-08-06 by reading the outcome family out before moving it, which is how the hero's copy was found in the same pass. Measured at 1440, dark: **the card's pair and the hero's** rest at `rgba(79,169,107,.12)` on `rgb(63,125,85)` with `rgb(119,209,155)` ink, so the two sides of the market state themselves before anybody presses anything. **`.bp-side`, 40 placements, rests at `rgb(36,40,47)` on a hairline with muted ink** - `--bg-control`, the system's quiet ground - and takes an outcome colour only when it is SELECTED, and its hover is **brass** rather than either outcome. **Both readings are defensible and that is the problem**: a card shows a market's two sides, a panel is a choice being made and arguably should not shout green and red before the choice exists. Nobody has decided which, and the same product does both. **And the panel disagrees with itself.** `.bp-dir .bp-side.sel` goes to solid `var(--outcome-yes)`; `.bet-dock .bp-side.sel` goes to `var(--outcome-yes-fill-strong)`, a lighter fill, on the same control in the same component one breakpoint apart. That is S35's shape - one concept spelled more than one way - in the outcome family, where `DESIGN.md` is most explicit that the colour carries meaning. **Closing condition: decide whether a chooser rests neutral or coloured, then decide one selected fill for the panel and the dock, then `.bp-side` can move to `components/yesno.css` as a face and the outcome atom reaches one file, distance 9 to 8.** Do not move it before: a partition that changes an outcome colour is changing the one thing `DESIGN.md` decides twice |
| ~~S50~~ | ~~**Ten brass tints are written as colour functions instead of tokens, seven of them at rungs the ladder does not have, and the ladder cannot see any of them**~~ **CLOSED 2026-08-07, AND THE TOOL MADE THE CHANGE, WHICH IS THE POINT.** The closing condition asked for `_rescale.py` to be taught to read a colour function so the next one cannot hide, and once it could read them it did the whole pass itself: `snap_longhand_tints()` masks comments, matches `color-mix(in oklab,var(--color-action) N%,transparent)`, and snaps N to the nearest rung of the brass ladder - COMPUTED rather than listed, because a longhand value can be any integer and a list would have to guess which one somebody types next. **10 rewrites in 7 files, on the first run:** 10% to `--tint-brass-09` twice, 14% and 16% and 20% to `--tint-brass-16` four times, 34% to `--tint-brass-30` twice, 45% to `--tint-brass-45`, 55% to `--tint-brass-60`. **WHAT IT DOES THAT A RENAME CANNOT.** An untokenised value has one alpha in both themes; a ROLE steps up a rung on chalk, because the same alpha over a pale stone does not read as it does over graphite. So this pass hands the light theme back to the ladder at ten sites that had been flat: **the checked filter 20 to 16 on graphite and to 30 on chalk, the profile tab 16 and 16 to 16 and 30, the sheet head and the how-it-works hero 34 to 30 and to 45, the avatar ring 55 to 60 in both** - that last is the only rung tokens.css leaves unshifted. Measured against its own ground in both themes, every ink stayed well clear of the floor: the checked filter went **5.49:1 to 5.87 on graphite** and 6.12 to 5.72 on chalk, the profile tab 6.73 to 6.73 and 6.33 to 5.72. **525 snapshots: 275 differ, 285 elements, and every one of them is one of the ten sites** - 160 readings of the count badge on a current lane, 60 of the two brass radials, 55 of the two selected tabs, 10 of the avatar ring. No box moved. The painted tree audits **byte-identical to the previous commit and clean at 0 findings**, which is the check that matters here: a tint stepping up a rung in daylight is exactly the change that could have crossed a contrast floor, and none did | Opened 2026-08-07 by closing S48, which found the eleventh. `ui-kit/_rescale.py` holds the brass ladder - 06, 09, 16, 30, 45, 60 - and maps every off-ladder spelling onto the nearest rung, `--brass-a14` to `--brass-a16` and so on. **It matches token NAMES.** A value typed as `color-mix(in oklab,var(--color-action) N%,transparent)` is brass at N per cent and is invisible to it, so an off-ladder rung written that way survives every rescale pass ever run. Swept over `components/`, comments stripped so a quoted value is not counted: **10%** in `chip.css` and `navitem.css`, **14%** in `chip.css` on `.chip-cnt` under a current lane - the exact twin of the one S48 moved - **16%** twice in `tabs.css`, **20%** in `filters.css`, **34%** in `dialog.css` and `hiw-dialog.css`, **45%** in `chip.css`, **55%** in `profile.css`. Only the 16s and the 45 are on the ladder, and those three are a naming debt rather than a repaint. The mixes into `--bg-control` and `--border-hairline` are NOT counted here: those blend two roles and are a different operation, not a rung. **Why it is a row rather than a sweep done on the spot.** Each off-ladder one is a visible value and the light theme is where it shows, because a tint that steps a rung on chalk moves by much more than the dark theme's three units - which is exactly the correction S48 measured, 1.11:1 to 1.25:1 against its own band. Seven repaints inside a de-duplication pass is the thing this document exists to stop. **Closing condition: one pass, each tint measured against its own ground in both themes before and after, and `_rescale.py` taught to read a colour function so the next one cannot hide** |
