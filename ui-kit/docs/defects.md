# The defect table

Step 8 of this stage is the check, and this is its first half: not a list of findings, but the map
of **where a finding could still be hiding**. The second half is the hunt, and it only has to run
where this table says nothing is holding.

**Why the first column is a gate and not a category.** The taxonomy this step inherits was written
for a project with no build check: eighteen classes of defect, each to be looked for by hand, on
every screen. This project has **38 gates over 97 checks**, and a green gate is not an audit result,
it is a **proof**. A class of defect covered by a gate does not need a person to go and look: it
needs a person to confirm the gate asks the right question, once, and then the answer holds on every
commit rather than on the day somebody checked. Running a manual pass over a gated class is not
thoroughness, it is a second, worse instrument reporting on the same thing.

So every row here is a class of defect, and the first column is what stands between the product and
that class today. Three statuses, and the third is the only one that costs time:

| status | meaning | what step 8 does with it |
|---|---|---|
| **PROVEN** | a gate in `ui-kit/_check_kit.py` asks it and is green | confirm the question is the right one; do not go looking |
| **MEASURED** | a browser instrument asks it and can exit 1, but it is not in the 1.5-second build | re-run it, record the number, and say it is not gated |
| **OPEN** | nothing asks it | **this is the hunt** |

The split of the instruments is deliberate and is written down in `ui-kit/_verify/README.md`:
`_check_kit.py` is Python that runs anywhere in about a second and a half, so everything in it runs
on every commit; `audit.cjs` needs a browser, a served tree and about a minute, so it runs when
asked. A check only some machines can run is a check nobody runs, which is why the two are separate
and why MEASURED is its own status rather than being counted as covered.

No em dash.

---

## The table

### PROVEN by a gate

| # | Class of defect | Gate | The question it actually asks |
|---|---|---|---|
| 1 | A declared class the product never wears | **30** | 105 painted screens as the corpus, 12 declared exceptions each with a reason, and an idle exception fails as loudly as an undeclared class |
| 2 | A selector with no markup anywhere | **14** | the reverse of 30, over every tree |
| 3 | A raw scale value in a component | **12** | plus: the stacking order is named, every consumer of a rescale moved, a distance is not a size |
| 4 | A colour that skips its semantic role | **13** | a component may not read a colour primitive; every screen can switch theme; every frame follows |
| 5 | An orphan token, or a `var()` with nothing behind it | **11** | both directions |
| 6 | A rule written on the element | **9** | `style=` fails, except the three declared non-styling cases: a datum, the event photograph, a run-time value |
| 7 | A whole imported before its part | **23** | the order is COMPUTED from specimen containment, not typed; a pattern carries no colour |
| 8 | A component with no stand, or a stand with no page | **2, 8, 24** | and the specimen shows everything the component contains |
| 9 | A missing interaction state | **25** | both directions against `_levels.STATIC`, and a state is a token, not a value |
| 10 | A generated page that no longer matches its generator | **29** | every generator still runs, 141 pages compared, and a second author is declared |
| 11 | A state photograph that is older than what it shows | **31** | a hash of the declared sources, not a pixel comparison; and a component with states and no picture is declared in `_states.NOT_SHOT` with the selector and the reason |
| 11a | **A state photograph whose frame is smaller than its subject** | **31** | the pad is derived from the element in the state being shot, and what the frame managed is written into the manifest beside what it needed, so a crop fails the build without a browser. Three declared tight frames, each with a reason, and an idle one fails |
| 12 | An authored page that is fluent and wrong | **32** | it exists or is declared absent; every section answered; every class and path it names is real; the anti-rule names a component and says Seen or Predicted |
| 13 | The two screen trees disagreeing | **18** | inside `<main>`, the header, the bottom nav, the footer and an invoked sheet, paired through `_twins.py`, with the six declared layer differences |
| 14 | One dialog with two markups | **19** | the canonical body is the one in `ui-visual/event-feed.html` |
| 15 | A heading level invented on a screen | **15** | one `<h1>`, no skipped level, read in BOTH trees |
| 16 | A path that goes nowhere | **4, 8, 21** | relative paths, registry pages, sidebar links, cross references, and no link into a raw `.md` |
| 17 | A request that leaves the repo before consent | **20** | no font host, every face committed, every page reaches them |
| 18 | An icon reference with no symbol | **3, 17** | and every mark is on the sheet |
| 19 | A rule of use with no source or no owner | **26** | and it is on the page of every component it names, in both directions |
| 20 | A pattern that is not one | **27** | three screens or it is a candidate; a page and a scene for each |
| 21 | A guide that invents its own sources | **28** | names every source, invents none, and is current |
| 22 | An em dash | **7** | the whole repo |
| 23 | A product edit that never got committed | **1** | `components/`, `wireframes/` and `ui-visual/` against HEAD, so a green build is a green build of what is actually in the tree |
| 23a | **A trader term where a person acts** | **33** | 13 terms crossed with 19 kinds of acting place, over both trees. Prose is not read at all, which is the invariant: the ban is about PLACE. Two declared exemptions, each a phrase rather than a location, and an idle one fails |
| 23b | **A document row that does not match the product** | **34** | the inventory's three computed columns are rebuilt and compared, and the hand-written half of the map fails on a row that matches nothing |
| 23c | **Markdown that survived the render** | **35** | every generated page, not the seven documents, because `inline()` is imported by the component and pattern generators too. Two marks that cannot be anything else on a rendered page: `](`, which only comes out of a link, and `**`, which only comes out of an emphasis that did not fire. It is here rather than under gate 21 because gate 21 re-renders the document and compares, so a defect in the RENDERER is reproduced on both sides and reads as agreement. It asks the markup and not the text - `<code>` and `<pre>` are stripped first, because this row quotes both marks and turned the gate red on the page describing it - and all 104 marks that shipped were in running text, so the strip costs nothing. Declared exceptions are pages that show markdown outside a quotation, and there are none; an idle one fails |
| 23d | **A gallery that is an occurrence rather than a difference** | **36** | no two state groups of one component may be the same answer, read across all four states in BOTH themes out of what each picture was taken at. That is the same sentence as "one base set plus one named difference each", in the form that can be computed: gate 32 already binds one caption to one group in both directions, so counting captions against groups is a count compared with itself. Two more directions: a picture that belongs to no group (it found two in `tabs`), and the five components still holding the defect, declared with what they carry so the debt is a register rather than a silence, and an entry that no longer holds a duplicate fails as idle |
| 23e | **A markdown table row that is not a row** | **37** | every body row of every table in every markdown in the repository must have its own head's cell count. A defect of FORM, which is why nothing here asked: every other check asks about meaning, and a row that is not a row resolves, exists, and matches. Two backlog entries written on one line rendered as ONE row of **7 cells against a 3-column head**, so `S28` was not an item at all and a reference to it by number resolved as text and not as structure; the neighbour was **2 cells** where the head has 3, the same defect the other way. **The corpus is every markdown, on gate 7's argument, and it earned that immediately**: scanning only the documents that have a page found the one it was written for, scanning everything found seven more in two files nobody would have opened. A blank line ends a table, so a data row rendering as a second head is caught by the same read. Declared ragged rows carry the usual idle control and there are none |

**29 classes, and none of them needs a person to go and look.** What step 8 owes each of these is one
question: does the gate ask the right thing. Two of the 23 have a known answer to that already, and
both are recorded rather than assumed: gate 14 missed `.signin-lead` and `.delta .row` because each
selector has a real ancestor, and gate 1 masks the `<aside>`, which is why gate 22 exists.

### MEASURED, and not gated

| # | Class of defect | Instrument | Standing |
|---|---|---|---|
| 24 | Contrast under the floor | `audit.cjs` | **FIXED 2026-08-03.** The 378 were one defect: `oddsbar.css` put the FILL roles on 12px bold text. `--outcome-yes-text` / `--outcome-no-text` already existed, so the repair is two words and no token moved. Re-measured in a browser, both themes: `span.l-yes` daylight 2.62 -> **7.23**, `span.l-no` daylight 3.76 -> **7.57**, `span.l-no` graphite 4.35 -> **7.54**, `span.l-yes` graphite 6.23 -> **9.82**. `ui-kit/docs/backlog.md` S14 is closed and `ui-kit/docs/history.md` records why it is the run's best evidence for the semantic token level. **The row stays MEASURED and does not become PROVEN**: nothing gates it, and the reason the same defect stood through five stages is finding 42 below |
| 25 | A link with no rule behind it | `audit.cjs` | 992 links, every one inside a component that styles it; the one exception became S3. **Re-run 2026-08-03 after this step's edits: 0 findings** |
| 26 | Horizontal overflow | `audit.cjs` | **Re-run 2026-08-03: 0 findings over 424 renders.** Not gated, so this sentence is true of that run and of nothing later |
| 27 | A request that 404s | `audit.cjs` | **Re-run 2026-08-03: 0 findings over 424 renders**, same caveat |
| 28 | A pinned box taller than the window it is pinned to | `audit.cjs`, second pass at 640px | three rails found and fixed; what the fix left behind is S12 and S13. **Re-run 2026-08-03: 720 pinned boxes measured at 640px of window, 0 findings** |
| 29 | The focus ring at every tab stop | `audit.cjs --focus` | 18 tab stops on the document page, all measured |
| 30 | A painted screen that moved when the system changed | `snap.cjs` | 525 of 525 identical across the button merge; 84,836 boxes unmoved across the pattern extraction |

**Seven classes that are true as of the last run and not on any commit.** Step 8 owed these a re-run
and a number, not a search, and the re-run happened on 2026-08-03 after every edit this step made:

> `node ui-kit/_verify/audit.cjs --screens` - **106 pages, 2 themes, 2 widths = 424 renders. 0
> findings.** 720 pinned boxes measured at 640px of window; 660 elements unmeasurable through a blend
> or a filter and 380 painting no glyph at all, both counted apart rather than passed.

**And that sentence is written with the caveat row 42 exists for.** A zero from this instrument is
what stood in three documents fifteen times while 378 elements measured under the floor. What is
different now is not the instrument's word, it is that the one thing it used to be wrong about was
found by hand, fixed at the role, and re-measured element by element in the browser - `span.l-yes`
2.62 to 7.23 and `span.l-no` 3.76 to 7.57 in daylight, 4.35 to 7.54 and 6.23 to 9.82 on graphite.
The zero above is only as good as its next audit, and it is not gated, which is why it carries a
date and a corpus instead of a claim.

### OPEN: nothing asks these, and this is the hunt

| # | Class of defect | Where it is already known to bite | Why no gate |
|---|---|---|---|
| ~~31~~ | ~~**Copy against the voice contract**~~ **CLOSED: it is gate 33.** | ~~`docs/backlog.md` 29: five placements of a trader term~~ - all five rewritten by `wireframes/_generators/voice_reconcile.py`, 97 replacements on 22 files across both trees, second run 0. `voice/docs/microcopy.md` Step 28 | **The gate's own two-way proof found what the rule could not.** Reverting each of the five one at a time demanded a red for each, and two came back GREEN: `Holders` was not in the lexicon at all, because the list held `position` and not the person who has one, and `Liquidity` and `(AMM)` were invisible to the scanner because `finditer` does not overlap and an enclosing `<div>` had swallowed its own children. Both are fixed, all five now go red one at a time, and `voice.md` gained `holder`. **A checker that cannot fail on the defect it was written for is row 42 in a new costume** |
| ~~32~~ | ~~**A document row that does not match the product**~~ **CLOSED: it is gate 34.** | ~~S21: inventory L163, L155 and L97~~ - all three corrected, and the MECHANISM closed rather than the instances: `stems_for()` took the first dot-separated part of a selector, so `p.pos-status` resolved to the tag and fell through to the hand map | **And the idle control found eight more the moment it was switched on.** Eight of forty one hand-map rows matched nothing, every one in the same hand-written half both false cells came from, and the rebuilt table is byte-identical without them. What is deliberately NOT gated is named in the code: the `#f` column, where 38 of 54 findable rows disagree with a count, because the cells carry two different definitions. `ui-kit/docs/backlog.md` S23 |
| ~~33~~ | ~~**A class declared in the wrong file**~~ **DONE 2026-08-03.** | ~~`docs/backlog.md` 17: five classes~~ - 17 rules moved across five pairs of files. `components/index.css` rebuilt from the computed order, four files moved. **525 snapshots, 105 pages at three widths, 0 differ, 0 elements changed** | **Still no gate, and now there is a reason to say so out loud.** The ownership map was never wrong: `.grid-l` in `feed.css` WAS the file styling it with the fewest ancestors, which is why nothing could see it and why this took a person. What the move bought is smaller than a gate and better than nothing: `_levels.ORDER_BREAK` lost two of its four hand-written cycles, because both were made OF a misfiled class rather than of a real nesting. **A hand-written tie-break is now readable as a finding waiting to be closed**, which is the nearest thing to a check this class can have |
| 34 | **A component boundary nobody has drawn** | `docs/backlog.md` 16b, 16d, 18, 19 | `card` is a molecule and an organism, `notice` is six blocks, `position` is four things, `hiw-dialog` is two components on one vocabulary. Arithmetic can count them; only a person can split them |
| 35 | **Keyboard reachability** | `docs/backlog.md` 22 (the filter panel) and 23 (a multi-outcome row is a `<div>` that answers a click) | `audit.cjs --focus` walks the tab order and measures the RING; it does not ask whether everything operable is IN the order |
| 36 | **What a screen reader is told when something changes** | `docs/backlog.md` 24 and 25 (eight error blocks announced to nobody) | Nothing in the build reads ARIA |
| 37 | **A promise the product cannot keep** | `docs/backlog.md` 27 and 28: 16 dead footer destinations on 104 screens, 1,664 links into nowhere, 8 of them on no map at all | Gate 4 resolves relative paths; `href="#"` resolves fine. A destination that exists as a page and not as a plan is an IA question |
| 38 | **A shipped block that no document describes** | `docs/backlog.md` 26: the featured hero, in both trees and in no document | Gate 18 pairs the trees and both carry it, so agreement is not evidence |
| ~~39~~ | ~~**One shape declared more than once**~~ **DONE 2026-08-03.** | S16, S17 and now S18: `--line-brass-soft` folded into `--border-brass-hover` through `ui-kit/_rescale.py` `ROLE_RENAME`, 9 rewrites across 8 files, one token gone, no value edited | **Still found by reading and not by a check, and the fourth instance says what a check would have to ask.** Two roles with the same value in every theme are not automatically one role - `--result-won` and `--outcome-yes` are the same green on purpose, and `tokens.css` says so at both declarations. What made this one a duplicate is that both DESCRIPTIONS said the same job. A gate would have to compare intent, which is the class of thing gate 32 can only check the shape of |
| 40 | **Repo weight and tracked artefacts** | `ui-kit/docs/backlog.md` S22: 19 MB of screenshots in git, in a 62 MB pack | The `.gitignore` rule that was written for exactly this cannot untrack what predates it and names one extension |
| 41 | **A number in a document that nothing computes** | S21's L97 is the instance; the class is larger | `_fill_inventory.py` computes three columns of the inventory and the screens column is not one of them |
| 42 | **A recorded measurement that was false when it was written** | **Fifteen separate "0 below AA" claims**, in three documents: `docs/decisions.md` (8), `ui-kit/docs/history.md` (6) and `voice/docs/microcopy.md` (1), over corpora up to "420 page loads x 2 themes, 86,534 text pairs". `ui-kit/docs/backlog.md` S14 measures **378 elements below 4.5:1** and says explicitly "Pre-existing: measured identical at HEAD before this pass and after it", so the 378 were there on every one of those dates | Nothing checks a document against a measurement, and this one cannot be closed by editing: `history.md` and `decisions.md` are written once and never edited on purpose, so the correction has to be a LATER entry that names the false lines. **WHY THE ZERO SURVIVED FIFTEEN PRINTINGS, and it is a rule rather than an episode.** The zero was printed by an instrument carrying three bugs this repo had already found and written down: a regex reading the components of `color-mix(in oklab, ...)` as sRGB bytes, a print limit that stopped at six findings per render (32 of 48 renders hit it exactly), and `mix-blend-mode`, which no computed style can resolve. Nobody lied. **A broken instrument that reports a DEFECT gets caught, because somebody goes to look and finds the instrument lying. A broken instrument that reports ALL CLEAR is caught by nobody, because there is nothing to go and look at.** The most dangerous output of a broken check is not a false alarm, it is a clean bill of health, and this is the one asymmetry that decides where verification effort belongs. **The sweep behind the number:** 79 claims of the "nothing was found" form across the repo's markdown - 29 gated today and therefore still true by construction (em dash to gate 7, broken links to 4, 8 and 21), 18 about contrast, and 30 measured by a browser and gated by nothing, true as of their run and unknown today |

| ~~43~~ | ~~**The instrument crops the subject of its own photograph**~~ **DONE 2026-08-03, and it is now the third half of gate 31.** | Measured, then fixed, then re-measured. **36 of 790 state pictures were short**, and every one of them was a FOCUS picture - the single state whose entire subject is a ring drawn OUTSIDE the box, `--ring` 2px at 2px offset, 4px a side. The pad is now DERIVED from the element being photographed, in the state it is in: `extentAt()` reads the computed outline and every non-inset shadow and returns the four sides, offset + spread + blur/2 being where the paint stops. **Over all 790 pictures the widest answer in the whole tree is 4px**, and no shadow on any photographed element reaches outside it - a number worth having measured rather than assumed, because the static reading of the stylesheet says 90px and the elements that carry those plate shadows are not the ones being photographed. The row gap is 24, which is twice the 12 `shoot()` asks for, so the halving caps nothing; the specimen page gained the 12px edge it never had. **36 -> 6 -> 0 undeclared**, the last 3 groups declared in `_states.TIGHT` with a reason each and an idle control | **Two more instrument defects fell out of fixing it, and both are the same shape.** A frame that ran past the last pixel of the document took the whole run down: a bet sheet is `position:fixed`, so it is painted in the viewport and is not in the document's scroll extent at all. And the clip and `boxAt()` had never agreed on a coordinate system - `boxAt()` returns DOCUMENT coordinates and a clip without `fullPage` is measured against the VIEWPORT image, which was invisible for as long as every specimen was too short to scroll. Giving the page a 12px edge made one scroll, and the API refused. Proved both ways before it was changed: the same clip fails without `fullPage` at scroll 0 and at scroll 138, and succeeds with it at both. **The bands are the other half of the fix**: two themes as full-width rules inside the gallery rather than two rounded boxes of a third ground, because a state picture is already a frame and a theme is a condition, not a card |

| ~~44~~ | ~~**One button under five names, and the size is encoded in the name of a PLACE**~~ **DONE 2026-08-05.** | The measurement is in `ui-kit/docs/backlog.md` **S24**, in full, as the system's largest open structural item: 5 names, 3 axes, 30 rules of which **13 are reconciliation**, 4 of those existing only to put an axis back and 1 re-declaring the whole anatomy in 16 declarations. 64 shots where the axes say 22 | **The measurement is the decision, and it argues against doing it.** Three axes do not fit inside `components/button.css`: the position mechanism belongs to `patterns/action-bar.css` and the `width:100%` to a `state-block` context, so consolidating touches a pattern and two components - a refactor of the centre of the system on the step that closes it. And the right answer for emphasis is `.primary` in the MARKUP, which is frozen, so the system would gain axes its own markup cannot express. **Closing condition was the markup thaw, and the thaw came on 2026-08-05.** Every argument above held except the one it rested on: the markup is not frozen, it is `wireframes/` and `ui-visual/`, and this repo changes both in one pass as a matter of routine. `.auth-btn`, `.state-btn`, `.provider-btn`, `.confirm-btn` and the action bar's bare `<button>` are now `.btn` plus an emphasis, a size and an optional `.btn-block`: **710 placements on 105 painted screens and 703 on 104 grey ones, migrated in the same change as the stylesheet**, which is the condition gate 30 states and the condition the first attempt at this vocabulary failed. The two objections were answered rather than waived: the position mechanism stayed in `patterns/action-bar.css` where it belongs, and the three width overrides became one class the person placing the control can read. `components/button.css` went from 30 rules to 20 declarations and from 11 scoped selectors to 1; the component drew 19 faces at 1440 and draws 13; its state galleries went from 5 to 3. What the page keeps is the matrix, now one specimen instead of two, because a form is a set of classes and no longer a place |
| 45 | **Six icon-buttons, six files, and none of them is the button file** | Counted over both trees, a `<button>` whose whole content is a mark: `.icon-btn` 388 (`header.css`), `.sheet-close` 228 (`dialog.css`), `.hiw-close` 105 (`hiw-dialog.css`), `.bookmark-btn` 84 (`card.css`), `.ed-act` 27 (`event-detail.css`), `.toast-close` 4 (`toast.css`). **709 in the painted tree, six classes, six files, and `components/button.css` draws none of them.** That is `ui-kit/docs/backlog.md` S11 at family scale, and S11 was one file. **And the letter is not a toast problem.** `.toast-close` ships `<button class="toast-close">x</button>` at 24x24 with `font-size:12` and `line-height:none`, so a glyph with its own baseline sits where a mark should - 4 buttons on 1 screen. `.sheet-close` ships the SAME letter on **123 buttons across 105 screens**, and ships an inline SVG on 105 others: one class, two answers, and in the painted tree every bare `dialog.app-dialog` gets the letter while `signin-dialog` gets the mark. Both trees agree on every count, so gate 18 is quiet and correct | **Gate 38 since 2026-08-04, and SIX BECAME FIVE on 2026-08-05.** The census in `ui-kit/_worn.py` counts every control in the product shaped like a button and fails the build on one it cannot name, on a kind nothing wears, and on a kind whose owner has no page; `ui-kit/button.html` carries the index. `.hiw-close` is gone: it and `.sheet-close` were one control in two files, and the merge left one class, one file and one census row. **The letter is gone with it**, and that half is worth more than the file count. All 245 of the buttons that shipped a bare `x` across the two trees now ship the same svg the other 209 already did, byte for byte, so the sentence above - one class, two answers - is no longer true of anything. Which also deletes the MECHANISM: `components/dialog.css` used to hide the icon and paint the cross with two pseudo bars so that one rule could cover both markups, and pseudo bars are the one thing in this product no query can select (row 54 is what that cost). Measured before and after over 105 painted screens: 666 placements, **four distinct faces became one**. What is still true is the rest of the row: `.toast-close` keeps its letter and its own ground on purpose, and `components/button.css` still draws none of these |
| 46 | **A link wrapped around a button, 2,448 times** | Found while measuring row 35's reachability. `<a href="..."><button type="button">YES</button></a>` is the shipped shape of every YES/NO on a feed card, and of the header's portfolio and favourites controls: **2,448 occurrences on 133 painted screens and 1,629 on 104 grey ones**, both trees agreeing. `<button>` is interactive content and the HTML content model forbids interactive content inside `<a>`; the two elements also carry different roles, so what a screen reader announces for the pair is not defined by anything. The largest single shape is the bare `<a href="..."><button type="button">` at 1,712, and the loudest is `<a href="..." aria-label="Favorites, see saved events"><button type="button" class="icon-btn" aria-label="Favorites">` - **two accessible names on one target**, 89 times | **None, and no gate can be written for it here.** Gate 4 resolves the href, gate 18 pairs the trees and they agree, and nothing in the build parses the HTML content model. It is markup, so under the twin contract it belongs to the grey tree first and is a Stage 12 handoff item rather than a Design System edit. Named here because it was measured here |
| 47 | **The ring reader measures the element that has focus, not the element the ring was moved to** | `ui-kit/_verify/browser.cjs` `ring()` reads `document.activeElement`, its `outlineColor`, and the ground under it. Two components in this system deliberately put the ring somewhere else, because the focused element is a hidden input: `tabs.css` maps `#edtab-*:focus-visible ~ .ed-tabbar label[for=...]` and `filters.css` now uses `label:has(input:focus-visible)`. In both, `base.css`'s universal `:focus-visible` still draws an outline on the hidden input itself - offscreen in one case, on a 1x1 transparent box in the other - so `ring()` returns `visible:true` and a ratio for a ring **no person can see**, and would return exactly that even if the label rule were deleted | **None, and it is the third instance of 42's shape in this run.** The instrument reports a clean result about the wrong object. Both ring placements in this repo were measured by hand instead, with the composite ground computed through the translucent brass wash: the tab labels at 8.71 and 7.14, the filter labels at 8.31/7.80 on graphite and 4.48/7.14 in daylight. A gate would have to ask "if this element's outline is invisible, is there another element that gained one" |
| 48 | **One idea done two ways in one file, and the way that already failed was left in place** | Found 2026-08-04 by re-capturing `tabs` under the extended face, which is the third verdict gate 36's pass allows for: a pair that differs by something it should not. `components/tabs.css` hides a CSS-only tab's radio TWICE, differently. Line 12 parks `.ed-tabradio` at `left:-9999px` (measured: a 13x13 box at x = -9941, opacity 1, pointer-events auto) and line 18 gives `.ptab-in` a 1x1 box at `opacity:0` with pointer-events none. The file's own comment says the first one painted the focus indicator off the left edge of the document and that the second was written so the ring lands in the right place and is then painted at zero alpha, **which is the same nothing**. So the second mechanism exists because the first one failed, and the first one is still there, one file away, waiting for the next `:focus-visible` rule somebody forgets to map by hand. **What the state pass keeps of them is two pictures of nothing**: a screenshot of a box 9941px outside the document and a screenshot of a 1x1 transparent box. Row 47 is the instrument half of the same story | **None, and this is what makes it a class rather than an instance.** A gate would have to ask whether two rules in one file are two answers to one question, which is intent. What is checkable is narrower and worth writing down as the closing condition: **one hiding idiom per repository**, declared once, and any second one fails. Nothing measures that today. The pair also does not merge under gate 36 and must not: the difference is real, it is just a difference nobody decided |
| 49 | **A face the instrument could not tell from its neighbour, because the key left out the thing that decides it** | Found 2026-08-04 by staging four scopes of `.provider-btn` in ONE specimen. `ui-kit/_verify/states.cjs` keyed a face on the element selector and its rest face together, and a rest face is nine values that do not include the font, so every provider row in the tree rests identically: the first one found in a document was measured and the rest were skipped as copies of it. While each scope had a document of its own this was survivable, because the first match in each document happened to be a different scope. The sign-in row's one pixel of hover lift was then **not merged away, but never measured** | **Closed.** The scope is in the key, and it cannot create a duplicate because the signature merge still runs after the shooting and still collapses two groups that agree in all four states in both themes. The re-capture took the system from 94 groups to 98 and gave four components a face the old key had been hiding |
| 50 | **A value read after the instrument disturbed its own subject** | `browser.cjs` `shoot()` took the face AFTER `page.screenshot({fullPage:true})`. A full-page screenshot is not a passive observer: Chromium scrolls the page to assemble it, the element travels out from under the pointer and back, `:hover` drops and re-applies, and the read that followed caught a 160ms transition about 3ms from its start. `rgba(199, 162, 78, 0.98) 0px 5.88701px 17.661px -7.84935px` where the settled value is `rgb(199, 162, 78) 0px 6px 18px -8px`, and 98 per cent of a glow is a different face to a signature comparison: it became a sixth gallery on the button page whose whole content was that a shadow had not finished arriving | **Closed.** The face is read once, before the screenshot, after a loop that stops when two consecutive reads agree. No wait placed before the screenshot could have fixed it, because the thing that disturbed the element happened after the wait |
| 51 | **A link with no rule, in the one place no sweep had looked: a table cell** | Found 2026-08-04 by asking 56 stand pages for the computed colour of every anchor that paints text of its own. The census section put 27 links into `td`s and all 27 came out at `rgb(0, 0, 238)`; `inventory.html` had a 28th that had been shipping in it, `<a class="tk-doc-ref">patterns</a>`, which is the same class that renders correctly everywhere else because everywhere else its text is inside a `code` span this file does colour | **Closed.** `.tk-tbl td a` replaced `.ck-anat td a` in the running-text link rule, which is the shape of the original miss: one TABLE was named where the CELL is the thing, and `.tk-doc-ref` now carries the colour itself rather than lending it to its child. 0 of 56 pages carry a User Agent blue that paints text |
| 52 | **A note that outlived the fact it recorded, and the stand was built on it** | `ui-kit/specimens.map.json` said `.provider-btn` and `.confirm-btn` have no ground outside a sheet and would render in the user agent's grey on the bare canvas. True when it was written and false one day later: the merge of 2026-08-03 put `background:var(--bg-control)` into the rule the whole family shares, and `components/button.css` says so in its own comment two lines above the rule. The button matrix was then built on the dead sentence and staged six controls inside dialogs and panels that decide nothing about them; **measured field for field against the same markup with no scope above it, twelve of the sixteen worn combinations are identical**, and `.confirm-btn` in an outcome dialog differs from the bare one in nothing at all. The page read as a scatter of cards, which is how it was reported | **Half closed.** The matrix is rebuilt around the control rather than the combination, and `ui-kit/_worn.py` `SAME` records each collapse with what was compared, held by gate 38 in both directions. **What is not closed is the class**: nothing in this build re-reads a note when the thing it describes changes, and there is no gate that could, because the note is prose about a cascade. The nearest thing is gate 32, which checks that an authored source EXISTS, not that it is still true |
| 53 | **A `<button>` read as the answer to "is this a button"** | Found 2026-08-04 by asking what `ui-kit/button.html` was actually showing. The census named every button-shaped control in the product correctly and then staged the wrong ones: a category chip, a tab strip, the filter toggle, a row of the account menu and five social marks stood live on the page called Buttons, because every one of them is spelled `<button>`. Measured over `ui-visual/`: **5,281 placements, of which 1,751 are actions**. The other 3,530 are 1,903 selector placements, 1,415 navigation and 212 of this vitrine's own chrome. **The selector is the case that makes it load-bearing rather than tidy**: a chip's subject is its group and its selected state, so one lifted out of its rail teaches a padding and nothing about the only thing it does | **Closed, and gated in both directions.** Every kind in `ui-kit/_worn.py` carries a role, and gate 38 now fails on an action that stands nowhere and on anything staged on the button page that is not an action. `IN_MATRIX` names the two kinds that are the page's own subject, so the gate cannot demand a second copy of them. The logo left `icon only, in the header` in the same pass: it was in that row for looking like a mark in the header band, and it navigates home |
| 54 | **A control put back in the flow lost the containing block its own glyph was drawn against** | Found 2026-08-04 by LOOKING at the rebuilt census, which is the only instrument that could have found it. `.kit-here` sets `position:static` on a picked control, because a close button is `position:absolute` in the sheet it belongs to and picked out of one it flies to the corner of the page. `.sheet-close` has no glyph and no visible `<svg>`: `dialog.app-dialog .sheet-close .ic` is `display:none`, the font is 0px and the ink is transparent, and its X is two absolutely positioned bars on `::before` and `::after`. Taking the button's positioning away took away the containing block those bars resolve against, so **both of them left the button and landed 700px down the page, over an unrelated row**. The census showed an empty circle and a stray X floating over nothing | **Closed.** `position:relative` with `inset:auto` lays out identically to `static` and keeps the containing block, which is the whole of the difference. **No measurement in this repo could have caught it**: every element involved measures correct, and the two that were wrong are pseudo-elements, which no query can select. It is row 42's shape once more, seen from the other side - not a broken instrument reporting clean, but a correct instrument with no question that reaches the object |
| 55 | **One specimen framed twice on one page, and two elements with the same id** | Found 2026-08-04 by reading the rebuilt page top to bottom. `live_specimens()` frames every specimen registered to the component, `census_table()` frames the census specimen under the role tables that explain it, and `button-census` is registered to `button` like everything else on the page. So the same twelve controls were rendered twice, about 900px apart, and the page shipped two elements carrying `id="sp-button-census"` - which makes every `#sp-` anchor and the jump link resolve to whichever the parser met first. **The section that owns a specimen and the section that frames everything were two lists with no relation between them** | **Closed.** `live_specimens()` subtracts `_worn.CENSUS_SPECIMENS`, which is the list the census section already reads, so there is one list and not two. The general shape is worth naming for the other 36 pages: a page section that frames a specimen by name has to be able to say so where the catch-all can see it, or the catch-all will frame it again |
| 56 | **A class belonging to the stand named a group belonging to the product** | Found the same day, by the gate rather than by eye, which is the whole point of it. `browser.cjs` names the scope of a measured control from the last class on the nearest scope element. Once the action bar carried `kit-nude` so it would give up its plate, that class WAS the last one, and the state group came back as `.cta-bar.kit-nude` - so gate 32 demanded a caption in `ui-kit/authored/button.md` for a scope the product does not have and never will. Writing that caption would have put a stand class into the documentation of a product control | **Closed, in the instrument and not in the caption.** Any `kit-` prefixed class is filtered out of the scope name, in `browser.cjs` where the name is made. It cost a full re-capture of all 746 pictures, which is `ui-kit/docs/backlog.md` S33 charging its third fee in two days, and it was still the right place: the same `kit-nude` treatment is about to reach 36 more component pages, and every one of them would have leaked the same way |
| 57 | **Two provider rows measured different min-heights and no rule sets one** | Left open rather than chased, and recorded so the next person does not chase it either. After `.provider-btn` was merged to one face, 431 of the 444 placements compute `min-height:auto` and 13 compute `0px`. Nothing in `components/button.css` declares min-height on this control, the 13 are spread over ten screens with labels that also appear in the 431, and the split is **byte-identical before and after the merge**, so it is not something the merge did. The nearest candidate is `dialog.app-dialog:modal .sheet-body{min-height:0}`, which is a different element and a pseudo-class that only a scripted `showModal()` raises | **None, and none is wanted yet.** It moves no pixel: every one of the 444 renders at the same height, and the two groups differ in no other property of the seventeen measured. It is on this list because the face comparison treats it as a difference, so `.provider-btn` reads as two faces on a page that says it is one, and a number that says two where the answer is one is worth a line even when the pixels agree |
| ~~58~~ | ~~**One control, two files, two mechanisms and two corners**~~ **CLOSED 2026-08-05** | Found 2026-08-05 by being told to look at it. `.sheet-close` (components/dialog.css, 228 placements) and `.hiw-close` (components/hiw-dialog.css, 105) are the same control: 32px box, `--radius-pill`, a 34 per cent `--scrim-photo` ground, a `--line-on-photo` edge, a 55 per cent hover, a 72 per cent press and the same transition, **every one of those declared twice**. They disagreed on two things and nobody chose either. The corner was `13px` in one file and `12px` in the other, and the first of those was a raw number with no token behind it. And the CROSS is drawn two different ways: one hides its `<svg>` and paints two absolutely positioned pseudo bars, the other centres the svg at 16px | **Values aligned; the merge is SCHEDULED, and the first reason given for not doing it was wrong.** Both corners are `--space-12` now, so the drift is closed. The reason first written here and in `components/dialog.css` was that CLAUDE.md starts a shared thing at three screens and two is a candidate: **that rule governs a COMPOSITION, a pattern of arrangement, and this is a component, for which the contract sets no screen threshold at all.** The counts were wrong for the quoted rule as well: `.sheet-close` stands on 105 painted screens and 104 grey, `.hiw-close` on 105 and 87. And the repo has made this exact move three times in this stage under one heading, one shape declared more than once and kept in step by hand: **S11 moved eight rules of `.cta-bar button` out of `account.css` into `button.css` because the control is a button.** What is true is only the timing: all three closes are actions by the census, the button reclassification is the step that gives every action its name and its level, and homing this control in a file now and in the vocabulary a step later moves the same thing twice. **The mark cannot be merged in either case**: 123 of the 228 `.sheet-close` buttons ship the letter x and 105 ship an svg (row 45), so the mechanism is decided by markup and waits for the thaw **DONE, all three of them.** One name: `.hiw-close` is `.sheet-close` on 192 buttons across the two trees. One file: every rule about this control is in `components/dialog.css`, and `components/hiw-dialog.css` keeps only what is genuinely its sheet's. One mark: the 245 buttons that shipped the letter `x` now ship the same svg the rest already did, so the pseudo bars, `font-size:0`, `color:transparent` and `.ic{display:none}` are all deleted - four rules and three declarations that existed only to cover markup the stylesheet could not fix. **And this file's own copy was TWO rules, not one**, split by `:not(.outcome-dialog)` against `.outcome-dialog`, which disagreed about `z-index`: the outcome copy had none and leaned on an anchor above it. So it was three declarations of one control and is one. **The sentence above about the mark waiting for a thaw was the same mistake as row 44's**: the markup is `wireframes/` and `ui-visual/`, this repo changes both in one pass as a matter of routine, and the svg is byte-identical in the grey tree and the painted one so there was nothing to reconcile. Measured over 105 painted screens at both widths, 666 placements: **four faces became one**, identical in box, inset, ground, edge, corner, z-index and mark. The one thing kept apart is the focus ring on the how-it-works head, because `.hiw-glow` puts brass exactly where the ring lands and a brass ring on brass measures 2.52:1 there; that rule moved into `dialog.css` with the control, scoped by the place, because it is a fact about a GROUND |
| 59 | **A width declared twice, and gate 9 was written to let the second one through** | Found 2026-08-05 by the migration, which is the only thing that could have found it: `.btn-block` gave the control a class that says it owns its row, and eight buttons across both trees went on carrying `style="width:100%"` as well. Gate 9 exempts `width:N%` by name, because a bar drawn to a width IS a datum and the exemption is right for the bar. On a button it is not a datum, it is **a rule in the one place the system cannot see, saying the same thing as a class the system can**. The grey tree had eight more of the same shape: `style="width:auto;padding:11px 14px;"` on the dock's Confirm, where `width:auto` is what an inline-flex control already is and the padding disagrees with `.btn-md` by one pixel. A third answer sat on `my-profile`, where `patterns/action-bar.css` already declares `.cta-bar>a>button{width:100%}` | **Closed by deletion, all 17, in both trees.** Not by tightening gate 9: the exemption is correct for the thing it was written for, and narrowing it to "a datum on an element that is not a control" adds a question the gate would have to answer about every element. What the migration shows is the general shape - **a vocabulary makes a redundant inline style visible by giving the same fact a name**, and the check that finds them is to grep for a `style=` attribute on an element carrying a class of the component you just built. It is now 0 on both trees |
| 60 | **The names were retired in the markup and left standing in the scripts that write it** | Found 2026-08-05 by grepping the whole repo for the five retired names rather than the two trees. Gate 14 asks whether a SELECTOR matches something and gate 30 asks whether the product wears what the system declares; **neither reads a python post-processor**, and three of them still spoke the old vocabulary. `ui-visual/_bet_drawer.py` is idempotent and meant to be re-run, and it writes `class="confirm-btn"` into two markup templates - so the next run would have put a retired name back on four shipped screens, silently, with every gate green. `ui-kit/_unfork.py` carried `.confirm-btn` and `.provider-btn` in `OUTSIDE_THE_CASE`, an exception list protecting selectors that no longer exist. `ui-kit/authored/state-block.md` told a reader the block's exit is a `.state-btn` | **Closed in all three, and the shape is worth naming.** The generator now writes the vocabulary and matches the Confirm by its EMPHASIS rather than by a place-name; the two dead exceptions are gone, because `components/button.css` no longer writes `.app-case` at all and a button selector can never become a candidate there again; the document says what the markup says. **A rename is not finished when the markup is renamed** - it is finished when nothing that WRITES the markup still knows the old word, and the gates in this repo look at what is written, not at what writes it |
| 61 | **An idempotent post-processor that was not at its fixed point, on main** | Found 2026-08-05 while proving the fix to row 60. `ui-visual/_bet_drawer.py` reported "updated 4 page(s)" on a clean checkout of `main` and again on every run: it moves the `uv-deposit` script block past the drawer script each time, so the file it produces and the file in the repo were never the same file. Checked on a worktree at HEAD to be sure the migration had not caused it - it had not | **Closed by running it, which is the entire fix.** The 4 pages now match what the generator makes, and a second run reports 0. Nothing rendered differently: two independent IIFEs binding different selectors, and their order decides nothing. That is exactly why it survived - **a drift that changes no pixel is invisible to every check in this repo except running the generator and looking at the diff**, and CLAUDE.md already states the rule this breaks, that every generator is at its fixed point, because a generator that copies a shell copies the shell's idea of where it is |
| 62 | **Two controls that answer the keyboard differently are one gallery, because a face has no ring in it** | Found 2026-08-05 by merging the two closes and watching the instrument report nothing. `ui-kit/_verify/browser.cjs` `face()` is nine properties - background, background-image, border colour and width, ink, radius, transform, box-shadow, opacity - and the OUTLINE is not among them. The merged close keeps exactly one difference, its focus ring on the how-it-works head, and that difference is measured, deliberate and documented: `.hiw-glow` puts brass where the ring lands, so a brass ring reads 2.52:1 there and the rule takes `--text-strong` instead. Measured with the element focused, the ring is `rgb(215,172,83)` everywhere else and `rgb(255,255,255)` there in the Vault, `rgb(104,79,24)` against `rgb(33,31,25)` in daylight. **The states pass went from 4 faces over 9 occurrences to 4 over 10**: it saw one more control and no more answers | **Named, not fixed, and the reason it is not a one-line fix is the finding's second half.** `ui-kit/docs/backlog.md` S40 carries it. The grouping key is taken at REST and in one theme, so adding `outlineColor` to `face()` would read the element's default rather than the value a `:focus-visible` rule sets - the property would be in the key and the difference still would not be. It needs the key taken in the focused state, or the ring made an axis of its own, and either edits `browser.cjs`, which `_states.ALWAYS` hashes, so it re-cuts every gallery in the vitrine and re-captures all 714 pictures. That is a change with a visible result across 24 components and it gets its own step. **What is done here is that the state is named where a reader would otherwise conclude it does not exist**, in `ui-kit/authored/dialog.md`, which is the shape `state-block.md` already uses for a declared state its stand cannot show |
| 63 | **An empty answer printed as an answer: the level said ATOM both when it had looked and when it could not see** | Found 2026-08-05 by being asked why Load more has a section of its own in atoms, why a toggle sits inside sort and filters, and why the inputs page shows buttons and a skeleton. All three are one cause. `ui-kit/_levels.py` computes a level from what a component CONTAINS, read out of the specimen DOM, and `_level()` returns 1 when that map is empty. **The map is empty in two unrelated situations**: the component really is one control, or the component is built entirely out of its OWN class names, and a map that knows only component names can then see nothing at all. The file's own docstring says so under "WHAT ARITHMETIC CANNOT SEE" and `RAISE` is the mechanism for answering it. What was missing is the check that anybody LOOKED: seventeen components have an empty containment, ten carried a RAISE with a reason, and **the other seven were the entire atom shelf, none of them ever examined**. `filters` holds a switch beside a disclosure over a radio panel; `input` holds a chip GROUP; `skeleton` declares `.sk-btn`, a placeholder named after another component's control. `ui-kit/docs/inventory.md` and the vitrine's side panel then printed all of it as a decision somebody took | **Gate 39, and the atom shelf went from 7 to 3.** An empty containment is now a question the build asks once per component, answered in `RAISE` (the map cannot see inside me, here is what is in there) or in the new `TRUE_ATOM` (the map saw nothing because there is nothing). Both directions: a component in neither fails, and a `TRUE_ATOM` row for a component the arithmetic CAN read fails too, since there the map has its own answer and the row is a second one. `button`, `oddsbar` and `loadmore` are the three that survive, and `loadmore`'s line says the level is right while the COMPONENT should not exist. **The cut itself is deliberately NOT in this pass**: naming a thing correctly and moving a class between files are different risks, and the second one moves the cascade. Backlog S41 carries it, with the proof each move has to produce |
| 64 | **A gate that knew two answers to a question with three** | Found 2026-08-05 by splitting `input`. Gate 25 asks whether an interactive component declares both `:hover` and `:active`, and it knew two answers: a component has both, or it is in `STATIC` and has neither. The moment `.quick` left `input.css` for `components/quick.css` it took the press with it, and the gate reported `input (has hover)` as a defect. **It is not one.** A text field has no press: the pointer going down on it moves the caret, and the state that follows is FOCUS, which that file declares twice on purpose, `:focus` for "the field is active, however you got here" and `:focus-visible` for the keyboard ring. The gate was about to make somebody invent an `:active` that answers nothing, which is the exact defect it exists to prevent, arrived at from the other side | **`NO_PRESS`, a third answer with a reason attached, and it is deliberately narrow.** It is not "components that may skip a state": every row names the state the control cannot have and why, and the gate still requires the OTHER one, so a row with no hover would fail. It runs in both directions like every declared list here - a component that says it has no press and then grows one is a claim that stopped being true, and `25 no idle row in the no-press map` is the check that says so. One row today: `input` |
| 65 | **Sequential prose laid out as parallel columns, on 39 of 39 component pages** | Found 2026-08-05 by the browser, and only because a NEW page overflowed. `ui-kit/toggle.html` pushed 11px sideways at 360 in both themes; the cause was not the code span the audit points at but the block around it. `.ck-rule`, which draws the RULE and the ANTI-RULE on every component page, was `display:flex` with no direction and no wrap, so its `<b>` label and **every `<p>` in it** were items of one row: two paragraphs sat side by side at half width, three at a third. Counted across the vitrine: 84 blocks, of which **43 hold one paragraph and worked by accident, 36 hold two, 4 hold three and 1 holds four - 41 wrong blocks over 39 pages**, which is every component page in the system. The other 40 were not overflowing because their text happened to be breakable, so nine passes and four browser sweeps went by without one of them saying anything | **A grid, and not a `flex-wrap`.** Wrapping would let the row break where the items stop fitting, which is a WIDTH question, and this is not one: two paragraphs of prose are sequential at 360 and at 1440 alike. Two columns, the label pinned to the first cell of the first row and every paragraph auto-placed down the second. A block with one paragraph renders exactly as it did, which is why 43 of the 84 are unchanged. Measured after: 0 overflowing elements on the page that reported it, at both widths, and the paragraphs went from 60px to the full column. **The general lesson is the one this repo keeps relearning from the other side**: the overflow was the symptom the instrument could see, and the defect was three times larger than the symptom. A page that does not overflow is not a page that is laid out |
| 66 | **The proof instrument was pointed at a different server, and two empty pages diff to zero** | Found 2026-08-05, and it had already been signed off three times before it was. `ui-kit/_verify/snap.cjs` loaded pages from `http://localhost:PORT/`; `ui-kit/_verify/audit.cjs` and every hand measurement used `http://127.0.0.1:PORT/`. Two python http.servers held port 8901 at once - one bound to `127.0.0.1` serving the repo, one bound to `*:8901` serving the PARENT directory - and macOS resolves `localhost` to `::1` first, so the snapshot tool got the second one and **every page it asked for was a 404**. An error page snapshots perfectly well: 6 elements, an `html` box of 380x200, Times at 16px. Two of those diff to ZERO, and zero is exactly what this tool prints when nothing moved. **Three passes were verified on it, one of them signed in the message of commit 67be096.** The real page, measured after the stray server was killed, is 1,211 elements and 380x7563 | **Two guards and one address, and the address is the smaller half.** The base is `127.0.0.1` now, which is what the audit already used, and being right about the address is not the same as being right about the server: the tool asks the HTTP status and refuses anything that is not 200, then counts the elements and refuses a page under 40, because a directory index and an error document both return content and both snapshot cleanly. **A run that cannot measure has to stop, not return zero.** Tested by pointing it at the wrong root on purpose: it stops on the first page and writes nothing. `diff.cjs` also stopped counting the serving origin as a difference, which is what turned a clean comparison of two worktrees into 700 changed background images. **Re-measured properly afterwards, both passes hold**: step A 525 snapshots 0 differ, step B 525 snapshots 0 differ. The claims were true; they were not proven when they were made, and that is the defect. It is row 42's shape for the fourth time - a correct-looking instrument reporting on something that is not its subject |
| 67 | **Five reading measures on one page, and the widest was the one nobody had set** | Found 2026-08-05 by screenshotting a component page and looking at it, then measuring what the eye had objected to. Read with the real `ch` unit over four component pages: a bare `<p>` inside a section runs at **99 characters per line**, `.tk-note` at 76, a `.tk-doc-list` item at 72, a gallery caption at 76, a rule paragraph at 63. **The 99 is the block with no `max-width` at all**, and it is the one that carries what the page is actually saying - the prose under WHAT IT IS and ANATOMY - because every other paragraph on the page happens to carry a class that had one. So the sentence a reader came for ran the full 1080px of the column while the footnote beside it was held to 675. Underneath that, `_page.css` declared the same intent with **six different numbers**, 60, 66, 72, 74, 76 and 80, none of them reading from the others, so the measure could not be changed without being changed six times | **One custom property, `--measure:74ch`, and 15 rules read it.** 74 because it is what the body-prose rules already used most, so the fix moves the fewest blocks that were already right. Two keep their own value and now carry the reason: the hero lead at 66, because a lead reads as a lead by being shorter than the body under it, and a choice-column paragraph at 60, because a measure wider than the column it stands in is not a measure. Re-read afterwards: 99 is gone and the page runs 65 to 74. `ui-kit/docs/backlog.md` S1 settled this same question for the DOCUMENT pages and never reached the component ones, which is why it read as closed |
| 68 | **A stand whose subject was the smallest thing on it** | Found 2026-08-05 by being told that nothing in the design system had visibly changed after four passes, which was true. Measured on the switch page: **5,618px tall, 13,040 characters, and the live control was 196px of it - three and a half per cent**. The page opened with two blocks of prose and put the component third, 750px down, after 1,900 characters about it. Its largest section, 26 per cent, was called *States, as they measure* and contained **no picture of a state**, two tables of rgb numbers instead; the second largest, 14 per cent, was the css source, which is in the repository. And the note above the component ran ten lines about frames, viewports and why a scope is present. The front door has the same shape: `overview.html` is 4,904px and the first rendered component appears after 1,250px of text cards. **This is a document about a design system rather than one** | **The thing first, and the machinery folded.** Live opens the page, so the component is at y=412 instead of 750 and is the first section rather than the third. A section note is now a SENTENCE, and everything explaining how the sentence was arrived at goes into a `+ HOW THIS IS READ` disclosure a reader opens on purpose: four notes moved, and the switch page lost 1,700 characters without a word being deleted. And a specimen got a STAGE, which the frame is not allowed to be: the rule under `.ck-frame` explains why that box carries no border, no corner and no ground, and the consequence had been a 40x24 switch floating in 1,150px of column with nothing under it. The stage goes on the figure one box out, where it can have an edge without touching the specimen, and it hugs its subject rather than spanning the column. Gates green, kit audit 0 new findings, product 525 snapshots 0 differ |
| 69 | **The evidence was standing where the reading should have been** | Found 2026-08-05, the morning after 68, by measuring the same three pages again in the browser rather than counting characters. Two sections were still the tail of every component page and both hold the same kind of thing: *States, as they measure* was **1,325px of the switch's 5,462 (24.3 per cent)**, 2,132 of the card's and 2,173 of the button's, and *What the file says* was another **834px (15.3 per cent)** on the switch, 1,397 on the card. Between them a third of the page. Neither is wrong and neither is optional: the rgb grid is the only thing that can catch a state token declared in one theme and not the other, and the selector table is what a person about to edit the file reads. **They are just not what a person opening the page of a switch came to read**, and most of the grid's cells say *nothing in the nine values moves* | **The numbers fold, the reading does not.** Each face now stands as a plate carrying its name, its authored caption and **one computed line naming what each state MOVES**: `press moves ground and edge; nothing moves on hover or focus`. The nine values are one click under it, unchanged and still stale-checked by `_states.py`, and the digest is the UNION of both themes on purpose, because a rule that fires in one and not the other is the defect that section exists to catch. Two states with the same move set are one clause, and the values are named in the order the table prints them. The state-rule table folds the same way the css source already did, under the heading that says both halves are for the same person. **95 groups over 26 components, 29 distinct digests**, and the wording was read on every one. Measured: switch page **5,462 -> 4,136px**, card **10,351 -> 8,178**, button **14,154 -> 12,271**; the States section **1,325 -> 659** on the switch and *What the file says* **834 -> 173** on all three. **And the line found something the table had hidden**: two of the 95 groups report `nothing moves on hover, press or disabled`, because the specimen stages a permanently disabled control, so the capture had been calling a disabled face `rest` and printing its own contradiction in a cell nobody reads. Gates green, kit audit **485 findings before and 485 after, the two sets identical**, product untouched by construction |
| 70 | **The front door opened with its own table of contents** | Found 2026-08-05 by measuring `overview.html` in the browser after the component pages were inverted. The page ran **Foundations, then The reasoning, then Every file, every page**: twelve TEXT cards over two sections, and **the first live component stood at y=1,250**. Every one of those twelve links is already a row in the tree on the left, which is on screen the whole time, so the first screen of the door into the system was a second copy of the navigation. Two cards in the grid also had no specimen - the token page and the pattern index, which are pages OF the system rather than components in it - and a grid row stretches every card to the tallest, so both stood as a 224px plate with a label at the bottom and an empty picture frame above it, **which is what a thumbnail that failed to load looks like** | **The system first.** The grid of live components is section 01 and the two card sections follow as what they are, ways in for a reader who wants the reasoning rather than the thing: first component at **y=592 instead of 1,250**, on the first screen at 900px. The section note is a sentence and the rest folds under `+ HOW THIS IS READ`, the mechanism from 68. A thumbless card is declared a door and centres its label rather than pretending to a picture. Gates green, kit audit **485 findings before and 485 after, the two sets identical**, product untouched. **And the instrument note, which cost an hour: `.ck-card` is `content-visibility:auto`, so a full-page screenshot photographs every off-screen card as an EMPTY BOX.** Chrome does not render a skipped subtree just because the capture is taller than the window. The page was read as "35 of 60 cards are blank" from exactly such a picture; the DOM says every card has its label, its path and a loaded frame, and a real viewport scrolled to the organisms band shows all sixteen. Same family as 66: **a photograph of this page is not evidence about this page**, and the reader is `vp.cjs`-style viewport shots or the DOM |
| 71 | **The system was never asked whether it knows the names the product wears** | Found 2026-08-05 by writing the reverse of gate 30. Nine gates walk system to product; **not one walked back**. For every class a painted screen carries, is there a rule in `components/` that reads it? The answer was seven, and the shape of the cost is gate 14's, seen from the other end: there a rule had no element, here an element has no rule, and a name with no rule is **indistinguishable from a component**. The next person reads `class="ed-chart"` off a screen, looks for `components/ed-chart.css`, finds nothing, and either invents the file or takes the name for something else, which is exactly what the six dead button classes were about to cost | **Gate 40, both directions.** `_adoption.UNSTYLED` declares each of the seven with one of three reasons, checked one at a time against the inline scripts and against every rule in `components/`: **four are hooks** a script queries and must not be deleted (`.ed-act`, `.ed-chart`, `.ed-chart-multi`, `.rules-panel`), **one is an exclusion** a rule makes on purpose (`.prov-google`: `button.css` recolours `.prov-x` and `.prov-apple` to the brand mark and leaves the Google logotype alone, because a recoloured logotype is the wrong mark), **two are debt** (`.ed-market`, `.toast-wrap`), written down as debt rather than dressed as a reason. A declared class that gains a rule fails as loudly as an undeclared one, so the cheapest way past the gate can never be a new line in it |
| 72 | **Three css headers were counting a JavaScript string as a placement** | Found 2026-08-05 while building gate 40, which reported `lg-item'+(i===sel?'` and `sel':'')+'` as classes the system does not know. The class reader was matching `class="..."` **inside `<script>`**, so a name a script writes at run time counted as a name an element carries. Stripping scripts changed the forward reading as well: `.l-yes`, `.l-no` and `.lbls` are on an element of **0 of the 105 painted screens** and named in the script of **all 105**, because the feed script builds the odds bar's labels out of the probability text, so **gate 30 had been passing three classes on the strength of a string literal**. `ui-kit/_adoption.py` already carried the answer one line up: `oddsbar` was declared "run time: the feed script builds the whole bar" | **A checker asks the markup, not the text**, which is the rule `CLAUDE.md` already states about a path inside `<code>`, arriving from a third direction. `<script>` is stripped from both the painted and the grey readings, and the three classes are declared run time beside the component they belong to. The correction is visible where it matters: **`oddsbar` stands on 9 screens and not 105, `catnav` on 57 and not 105, `options` on 14 and not 25.** All three headers are generated, so all three had been telling every reader that a component stands on every screen in the product |
| 73 | **One control written into two files, byte for byte, and three instruments had already said so** | Found 2026-08-05 by starting the chip migration with a measurement instead of a rewrite. `components/comments.css` styled `.cmt-controls .seg button` and `components/tabs.css` styled `.ed-range button`, and the four painted rules were **the same declaration block twice, property order and all**: `border:0; background:transparent; color:var(--text-muted); border-radius:var(--radius-10); padding:var(--space-4) var(--space-12)` and so on, plus the same hover, the same press and the same `.sel`. **Nothing was hiding.** `ui-kit/_states/index.json` had measured both at `rgba(0,0,0,0)` ground, `rgb(164,157,143)` edge and ink, 10px corner, and both selected states at `rgba(199,162,78,.16)` on `rgb(215,172,83)`; `ui-kit/_worn.py` filed them as two kinds of one ROLE with one face; `components/tabs.css` argued in its own margin that the rail should join the chip family. Three readings on three pages, and nobody had put two of them side by side | **`components/chip.css`, and the shape the buttons got rather than a file move.** Neither file could take the other, because a file that styles another component's classes is what S11 forbids, so the control is one marker class and the markup of BOTH trees moved in the same change: **63 buttons per tree over 9 screens each family, 126 in all, plus 7 in the frozen kit** so its specimens go on rendering. `.seg` and `.ed-range` keep the TROUGH and the rail, which is what they are: `comments.css` keeps the `--bg-well` well and the grey divider that draws the segmented look in the wireframes. The census answers immediately: **26 kinds of button-shaped control become 25**, and two rows of 27 and 36 become one of 63. The stand shows BOTH rails in one document on purpose, which is the thing no stand did before and the reason the duplicate survived a stage. **And it is one of four, not the four**: `.load-more` and `.feed-inner .cat-nav button` are the raised chip and are four declarations apart rather than zero, `.quick button` is a third face, the pill `.cat-nav button` a fourth. Backlog S41 keeps the rest and this file is where they come, each on its own evidence |
| 74 | **A rule kept for a tree that cannot see it, waiting for the day something stopped outranking it** | Found 2026-08-05 by `snap.cjs`, in the middle of the merge above, and by nothing else: **45 screens and 180 elements changed** where the merge was supposed to change nothing. `components/comments.css` carried `.cmt-controls .seg button{border-right:1px solid var(--border-hairline)}` and its `:last-child`, kept as the grey layer, the divided segment a wireframe draws. **They had never reached a wireframe.** `wireframes/` links no stylesheet from `components/` and carries its own `.seg button{border-right:1px solid #999}` inline, which `ui-kit/_adoption.py` has said in its own comments since it was written. The only thing those two rules ever did was LOSE, at (0,3,1) against the painted rule's (0,2,1). The moment the chip moved to its own file the painted rule became `.app-case .chip` at (0,2,0), the grey rules won, and a 1px right border arrived on 90 chips in the shipped product | **Deleted, not outranked.** Bumping the new component's specificity to win would have kept a rule alive whose entire purpose was to be beaten, in a file that no longer owns the control. The re-measure is **525 snapshots, 0 differ, 0 elements changed**. The same reasoning was then applied forward: the draft of `components/chip.css` opened with an unscoped `.chip{cursor:pointer}`, copied from the note `quick.css` carries about keeping a first layer, and it was cut before it shipped, because the only documents that carry `.chip` AND load this stylesheet put it inside `.app-case`. **A grey layer is not free and it is not inert**; it is a rule at a specificity nobody chose, and the day the paint moves it decides the product. Both files now say so where the rules used to stand |

**Twelve open classes**, three of them found while fixing the others, and six struck: **31 and 32 are
now gates 33 and 34**, so they are proofs rather than a pass; **33 and 39 are done** - both by a
person, because both are judgements no checker could reach; and **43 is done and became a gate**,
which is the one of the five that closed in both ways at once. Five (34, 38, 42, 45 and 47)
still need a person, and 44 is struck a sixth way - measured, decided, and deliberately not paid; four (35, 36, 37, 46) belong to owners outside this stage, three of them already
assigned in `docs/backlog.md` and the fourth a markup question the twin contract sends to the grey
tree first; two (40, 41) are hygiene with a measured size and a decision still to take.

**35 and 36 moved while this was being written.** 35's blocker was `docs/backlog.md` 22, and it is
closed: the filter panel is reachable, measured before and after with a real Tab walk. 36 is
narrower than it was for the same reason - the panel's three radiogroups were `display:none`, so a
screen reader was told there was a group of options and then given **0 of them**; the accessibility
tree now carries **5 radios with their names and their checked state** where it carried none. What
remains under 36 is the form-error axis, which is markup and is `docs/backlog.md` 24 and 25.
**42, 43 and 47 are one shape seen three times** - a broken instrument reporting a clean result, once
in a document, once in a photograph and once about a focus ring nobody can see. That is the argument
for where the next pass looks: not at what the checks report, but at what they are looking at.

**Row 35 moved while this table was being written**, and it is the reason the "which gate catches it"
column is worth the trouble. Of the three controls it named, two were not defects: `.rules-panel` is
not a control at all (the control is `.rules-tab`, a real `<button>` on all 18 instances), and the
four `<span class="bp-side">` are skeleton placeholders on two loading screens, which must not be
focusable. The third, `.opt-row`, is a control on 2 of its 14 screens and **has a working keyboard
route** - Enter and Space on the inner button produce a state identical to a mouse click, measured
field by field. The one real blocker was the one already written down as `docs/backlog.md` 22, and it
is now closed.

### Deferred by owner, and not this stage's hunt

`docs/backlog.md` items 3 (page scripts as code), 5 (page weight, font swap, layout shift) and 12
(odds-delta animation) are assigned to Stages 11 and 12. They are in no status above because they
are not open findings, they are work with a date on it.

---

## What this table changes about the run

The inherited plan was eighteen classes, hunted by hand, over 105 screens in two themes at two
widths. The count that matters is different: **29 classes are proven, 7 are measured and 12 are open** -
and of the eleven, five are the ones that actually need eyes. Two of the eighteen inherited classes
became gates in this step rather than a hunt, and both of the new gates found a defect the class
description had not known about, which is the argument for the column restated: a gate is not a
tidier way of looking, it is a different instrument.

That is the whole argument for putting the gate in the first column. A pass that re-hunts a gated
class is not more thorough than the gate; it is a person doing by hand, once, on the screens they
remembered to open, what a machine does completely on every commit. The gates were written over nine
audit passes precisely so that this step would be short.
