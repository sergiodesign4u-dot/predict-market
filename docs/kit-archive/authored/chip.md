# chip

## Sources

- `components/comments.css` and `components/tabs.css`, where this control lived until 2026-08-05 and where it lived TWICE. `.cmt-controls .seg button` and `.ed-range button` were the same declaration block byte for byte, property order and all, plus the same hover, the same press and the same `.sel`.
- `ui-kit/_states/index.json`, which had measured both and printed the answer on two different pages without anyone putting the two readings side by side: `rgba(0, 0, 0, 0)` ground, `rgb(164, 157, 143)` edge and ink, 10px corner, and both selected states at `rgba(199, 162, 78, .16)` on `rgb(215, 172, 83)`.
- `ui-kit/_worn.py`, which filed them as two kinds of one role: **chip, a sort segment** at 27 placements and **chip, a chart range** at 36. Same role, same face, two names.
- `ui-kit/docs/backlog.md` S41, which owns the chip family, and S35, which settled what CHOSEN looks like for all five faces at once. S46 answered the last one.
- `components/quick.css` and `components/header.css`, which drew the amount and the header strip until 2026-08-06, and `components/catnav.css`, which drew the other three until 2026-08-07.
- `DESIGN.md`, twice: "active is a brass tint" under Chips, and the One-Accent Rule. Both decide the selected state below.

## Purpose

A label a person picks between, when the choice is small, immediate and does not leave the screen. Four time ranges under a chart, three sort orders over a comment thread, eight categories in a strip, twelve sub-categories in a rail, five amounts on a sheet. It carries a value rather than doing a thing, so what it looks like afterwards matters more than what it looks like while it is pressed.

## Anatomy

- `.chip` - the atom, and it is four declarations: a pointer, the body face, no underline and a centred label. The last two arrived on 2026-08-07 with the ELEMENT: a chip that navigates is an `<a>` now rather than a `<button>` wrapped in one, and both values were things the `<button>` tag had been supplying for free - one of them written down twice in two other files, about the wrapper. Everything a person can see belongs to one of the five faces below, and **a base class is the atom or it is a face, never both** - the day this file's one face was written on `.chip` itself, 722 elements took values nobody had chosen for them.
- `.chip.chip-rail` - a segmented control on a plate: no edge, no ground, `--text-muted`, a 10 corner, 12px semibold.
- `.chip.chip-quiet` - a scrolling strip: transparent on a transparent 1px band, a pill, 13px. The band is not decoration - it means the box is the SIZE's answer, so a chip does not grow by 2px at the moment it is chosen and given a brass edge.
- `.chip.chip-nav` - the category band: `--bg-chip` on a `--bevel-notice` edge at 14px, the loudest chip in the product, because on a phone it is the one control that changes what the page is about.
- `.chip.chip-lane` - the sub-category rail, and the only face in this system that answers TWO surfaces: a hairline pill in a scroller at 380, a full-width row with the count pushed to the far side above 900. The face carries that media query, because what changes with the window is the surface and not the placement.
- `.chip.chip-amount` - a form control in a sheet: `--bg-control` on a hairline, a 10 corner. What a chip looks like when it is one of the things you fill in.
- `.chip-ic`, `.chip-cnt` - the two parts. A category's mark, drawn in `currentColor` so the chip's ink is its ink, and a sub-category's count, mono because it is a figure. Both are here rather than with the rail for a reason gate 24 supplied: they change with the CONTROL's state, which is what a part of a face does, and leaving them behind made the atom read as containing a component.
- `.chip.sel` and `li[aria-current="page"] .chip` - the chosen one, and they are one rule. A brass TINT, brass ink, bold, a halo. `.sel` where a class survives the pointer leaving, `aria-current` where the chip points at the page you are on, which is the correct thing for navigation and is not going to be replaced by a class.

## When to use

For a set of two to five values a person switches between on the spot, inside a container that gives them their ground: a trough, a rail, a segment. For a set that has to be scrolled sideways, the strip faces exist and say so.

Not for a set that has to be scrolled and read at once. Five is a rail, twelve is a menu that has forgotten it has one.

## Rule

**A selected chip is a tint, never a fill.** A solid brass ground is the primary action's own face, and a rail of chips is not asking anybody to commit to anything. `DESIGN.md` decides it twice, under Chips and under the One-Accent Rule, and `components/quick.css` records what it cost the one time a chip took the fill: a lit brass $20 sitting directly above a lit brass Add funds.

**One recipe for chosen, and it is one rule for the whole family.** `--tint-brass-09` inside a `--tint-brass-45` edge with `--text-brass-chip`, bold, and a halo that is a glow with negative spread rather than a lift. The edge is written where the face HAS one: the recipe colours what a chip already has and adds no parts, so the rail chip is borderless at rest and stays borderless and no box moves.

**The container gives the ground, not the chip.** Every rule in `components/chip.css` is scoped to `.app-case`, and the three quiet faces paint no ground at rest. That is what lets one control stand in a `--bg-well` trough, on a stone band and on a bare section without any of them looking wrong.

**A face is an answer to the surface the control stands on.** Five faces, five surfaces, and the fifth answers two of them because its surface changes with the window. That sentence is the whole map: it is why the amount is not the rail chip with a ground, and why reading the lane at one width put it in the wrong atom for a day.

## Anti-rule

Never draw it again in the file of whatever holds it. That is exactly how it came to exist four times: `comments.css` owned the sort segment because the segment stands in the comment controls, `tabs.css` owned the chart range, `quick.css` owned the amounts, `catnav.css` owned three more, and every one of those files was right about its container and wrong about the control.

Never give the atom a face. `.chip` carries what every chip agrees on and nothing else. A sixth surface takes a sixth face; a difference of four pixels takes none, which is what the Trending sub-filter turned out to be against 525 identical chips in the header.

Never let a part stay behind. If a chip grows something that lives inside it and changes when the chip does, it comes here and takes a chip name. Gate 24 will say so anyway, and it is faster to hear it from this paragraph.

Seen: `ui-kit/docs/backlog.md` S35, S38, S41 and S46, and `ui-kit/docs/defects.md`, which records the measurement that ended the duplicate.

## States

- `button.chip.chip-rail @chip-rails` - The rail chip at rest: no ground, no edge, muted ink. Hover brings the ink up to `--text-primary` and nothing else moves, which is the whole idea of a quiet control. **This one block is five buttons in two different rails**, three in the comment sorter's trough and two in the chart range, and the capture merged them without being asked: they measure identical in all four states in both themes. That merge is the proof the two files were drawing one control, taken by an instrument rather than by a person reading two stylesheets.
- `button.chip.chip-rail.sel @chip-rails` - **The same chip chosen**, and again one block for both rails: `rgba(199, 162, 78, .09)` with `rgb(231, 214, 166)` and the halo, and **no edge**, because the face has none at rest and the recipe adds no parts. **It answers the finger since 2026-08-07**, when S38 closed: the ground settles onto `--bg-pressed` and the brass ink, the weight and the halo stay exactly where they were, so what a person sees is the stone going in rather than the choice coming off. That was never this face's decision to take, and it was not a new one either - the `[aria-current]` chips already had it, and `.sel` was excluded by a `:not()` and by nothing else. It answers no POINTER, and that is now a decision: all six chosen controls in the product were measured and not one of them does, because a chosen face is already the answer.
- `a.chip.chip-nav @catnav-chips` - **The category you are in.** The chosen recipe on the loudest face in the family, and the numbers are the same eight the rail chip and the amount show: `rgba(199, 162, 78, .09)` inside `rgba(199, 162, 78, .45)`, `rgb(231, 214, 166)`, the glow at `-8px` spread. What is different is that **this one answers the finger**: `active` drops to `rgb(25, 27, 31)` while keeping every brass value. That is deliberate and it arrived on 2026-08-07 - on a phone this strip is the only way to change category, and the chip most likely to be tapped is the one you are already on, because tapping it is how you get back to the top of it.
- `button.kit-here.chip.chip-nav.load-more @button-census` - **A category you are not in, and the Load more button, in ONE group, which is this pass's whole proof.** The capture spans three specimens - the button census, the load-more stand and the category strip - because on 2026-08-07 `.load-more` stopped being a second drawing of this chip and started carrying it. They measure identical in all four states in both themes, which is what a merge in this gallery means and is the same evidence that ended `.seg` against `.ed-range`. Rest is `--bg-chip` on a 6 per cent bevel, brightening to `rgb(32, 36, 42)` under the pointer with a brass edge and settling to `rgb(25, 27, 31)` under the finger: lighter on hover, darker on press, the right pair of directions for a chip with no lift to flatten. The focus offset is **negative** here and nowhere else in the family, because the category strip scrolls sideways and its `<ul>` clips at a 2px padding box: a ring drawn outside the chip lost its top and bottom runs and rendered as two vertical bars. Load more inherits that ring and does not need it, which is the one thing the merge costs and it costs nothing a person can see.
- `button.chip.chip-quiet @catnav-subfilter` - **The Trending sub-filter you are on.** The same recipe again, on the pill corner, and this group is the reason the sub-filter is not a face of its own: measured against the 525 quiet chips in the header it differed by four pixels of vertical padding and nothing else, so it adopted and grew 30px tall to 38. Its chosen weight went 600 to 700 in the same move, which is one value on one element, and it is the family's.
- `button.chip.chip-quiet @catnav-subfilter (2)` - **A sub-filter you are not on, all four faces.** Transparent on a transparent band, and the two answers travel opposite ways on purpose: hover is a white wash at 6 per cent, press is `--bg-pressed`, which is opaque and sits BELOW the plate the chip stands on. On a ground with no fill of its own that is what pushed in looks like.
- `button.chip.chip-lane @catnav-subcat` - **The sub-category you are on, and the last of S35's five spellings.** It said chosen with 18 per cent of `--color-action` in oklab, a solid brass edge and semibold, because on the day the other four were unified this control was filed under a different atom. It wears the recipe now: 160 readings changed colour, ground, four border colours, the halo and the weight, and the count inside it followed its label from 600 to 700. The one pixel that moved anywhere in this pass moved here, a chosen lane going 94px wide to 95 because bold is wider than semibold. **AND SINCE 2026-08-07 THIS BLOCK IS THE CHOSEN AMOUNT TOO, which is what closing S38 proved.** The deposit sheet's chosen $20, the bet panel's and the bet sheet's had their own group in this gallery for as long as they answered nothing under a finger while this one settled onto `--bg-pressed`. Give both the same press - one selector, `.app-case .chip:active` - and the instrument folds them into one reading: identical ground, edge, ink, corner, weight and halo in all four states in both themes. **A group did not disappear because somebody deleted a caption. It disappeared because two controls became the same control**, which is the same evidence that ended `.seg` against `.ed-range` and the three spellings of a chosen outcome.
- `button.chip.chip-lane @catnav-subcat (2)` - **A sub-category you are not on, all four faces, and the frame is 900px so this is the DESKTOP surface**: a transparent edge, a 10 corner, the row full width with the count pushed to the far side. Its hover is the only one in the family that tints in oklab, a 10 per cent `--color-action` wash inside a solid brass edge. **What this stand cannot show is the other half of the face.** At 380 the same control is a 114x41 hairline pill in a horizontal scroller, and a specimen has one width: the two readings are in `ui-kit/docs/atoms.md`, where the decision was taken, and reading only one of them is what put this control in `navitem` for a day.
- `button.chip.chip-amount @dialog.app-dialog` - **The amount face, and it arrived on 2026-08-06 from `components/quick.css`.** A chip on a hairline over `--bg-control` with a 10 corner: what a chip looks like when it is one of the things you fill in on a sheet rather than a way of filtering what you are reading. The deposit sheet's copy and the bet panel's measured identical at rest and are one face. **Its hover was decided rather than carried**: the deposit sheet had one and the bet panel had none, the same face on the same control, so the face took the hover and both places answer now. That change lives entirely inside `:hover`, so no before-and-after in this repo can see it and this gallery is the only thing that can.
