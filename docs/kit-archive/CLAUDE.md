# docs/kit-archive/ - do not update this folder

Frozen on 2026-08-07. These are the documents and the per-component writing of the vitrine that was
deleted that day. **Nothing reads them**: no page, no script, no check.

- **Do not edit a file here to keep it current.** It was true on the day it was written and that is
  the only claim it makes. If it disagrees with the repository, the repository is right.
- **Do not link a live page into this folder** except from `README.md` and from the notes that
  explain what happened.
- **Do read it before re-measuring something.** `README.md` names the four things worth taking
  forward: the level formula, the S34 size measurement (9,648 readings, 66 distinct faces of one
  control kind), the rule and anti-rule per component in `authored/`, and the defect log.

New writing about the system goes to `../../ui-kit/docs/`, which is where the rebuilt kit keeps its
census, its inventory and its consolidation record.
